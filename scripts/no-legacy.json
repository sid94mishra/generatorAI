{
  "$comment": "Banned identifiers per workflow-overhaul phase (README §7 step 6, R-1). Each phase that deletes a legacy path APPENDS entries here in the same commit, so the name can never come back. `pattern` is a JavaScript regex source, matched per line against packages/** and apps/** source (tests, dist and node_modules excluded). `paths` (optional) limits an entry to path prefixes; `phase` and `reason` are for humans. Starts empty in P00.",
  "banned": []
}
