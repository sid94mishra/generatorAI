// Provider capability levels (P02; RV-6, RV-9). The table is pure data in
// @generatorai/shared so the web session editor reads the same levels the
// composer decides by.
export {
  PROVIDER_CAPABILITY_LEVELS,
  capabilityLevelsFor,
  type CapabilityLevels,
  type LevelledProviderId,
} from '@generatorai/shared';
