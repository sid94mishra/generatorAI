{
  "name": "@generatorai/desktop",
  "version": "0.0.1-alpha.1",
  "private": true,
  "description": "GeneratorAI Desktop — native Electron shell with full parity to the web UI and CLI",
  "author": "GeneratorAI",
  "license": "MIT",
  "type": "commonjs",
  "main": "dist/main/index.js",
  "scripts": {
    "build:bundle": "tsup",
    "build:resources": "node scripts/build-resources.mjs",
    "build": "pnpm run build:bundle && pnpm run build:resources",
    "dev": "node scripts/dev.mjs",
    "preview": "pnpm run build && cross-env GENERATORAI_DESKTOP_MODE=standalone electron .",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run",
    "build:server": "pnpm --filter @generatorai/server... build && pnpm --filter @generatorai/server bundle && node scripts/stage-server-runtime.mjs",
    "prepack:dist": "pnpm --filter @generatorai/web build && pnpm run build && pnpm run build:server",
    "electron-builder": "node scripts/electron-builder.mjs",
    "verify:release": "node scripts/verify-release-manifest.mjs",
    "mock-update-server": "node scripts/mock-update-server.mjs",
    "package": "pnpm run prepack:dist && node scripts/electron-builder.mjs",
    "package:dir": "pnpm run prepack:dist && node scripts/electron-builder.mjs --dir"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "electron-updater": "^6.8.9",
    "node-pty": "^1.1.0",
    "playwright": "^1.58.2",
    "ws": "^8.21.3",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^22.10.0",
    "@types/ws": "^8.18.1",
    "cross-env": "^7.0.3",
    "electron": "42.8.0",
    "electron-builder": "^26.15.7",
    "playwright-core": "^1.58.2",
    "tsup": "^8.3.5",
    "yaml": "^2.8.3"
  },
  "optionalDependencies": {
    "@trycua/cua-driver": "0.19.3"
  }
}
