# Implementation tracker

This file tracks the implementation of every work package (WP) in the overhaul plan. It is the task-level companion to `STATUS.md`, which holds the phase and migration tables.

**Working copy:** git worktree `C:/gaiwf/repo`, branch `wf/overhaul`, created from `desktop_redesign` @ `a83a7bf`. It is local only and never pushed.

**Loop per phase:**
1. Implement every WP, with one commit per WP.
2. Run the §7 gate.
3. An independent review agent audits the phase against its plan file.
4. Fix the findings, then re-gate.
5. Mark the phase **done** here and in `STATUS.md`.

**Legend:**
- `todo`: not started.
- `wip`: in progress.
- `done`: implemented, with tests passing.
- `reviewed`: the independent review passed and its findings are fixed.
- `deferred`: deferred by a plan decision. The reason is given in the notes.
- `n/a`: not applicable.

## Phase summary

| Phase | Title | Impl | Review | Gate | Commits | Notes |
|---|---|---|---|---|---|---|
| 00 | Baseline and safety net | done | reviewed | pass | cbb9fb6..d772ee3 | Gate passes with the baseline exceptions recorded in STATUS.md. Deviations in DEVIATIONS.md (7 P00 rows). |
| 01 | Spec v2, legacy purge, definitions | done | reviewed | pass (2026-09-25) | 25fd04c..ebb4e82 | Part A (WP-1.1–1.4), part B (WP-1.5) and part C (WP-1.6–1.9) done. Gate: typecheck 52/52; tests = baseline failures only (10: cli 5, core 1, git 2, server 2; the core CRLF and POSIX-path baseline failures are fixed); lint green; no-legacy 78 bans, 0 hits, 11 comments (baseline lowered to 11); migrations-lock 55 locked; db-baseline v55; check:workflow-spec clean; fresh DB + dbcopy-upgrade pass (STATUS). |
| 02 | SessionComposer | done | reviewed | pass (2026-09-25) | 0c02b1c.. + "P02 review fixes R1-R9" | WP-2.0–2.11 done; review R1–R9 fixed (Phase 02 review below). One SessionComposer for chat and stage; binder, resolveMcp, agent projection, workspace exposure, gates, permission source, TurnRecorder, v56, SessionSpecEditor. Gate: typecheck 52/52; tests = baseline failures only (cli 5, core 1, git 2, server 2) after one new failure fixed (server automation test needed the PD-18 mode); lint green; no-legacy 90 bans, 0 hits, 11 comments; migrations-lock 56; db-baseline v56; BaselineFreshDb 10/10; dbcopy-upgrade v52→v56 chat rows unchanged. Live E2E not run (advisory). 11 P02 deviations in DEVIATIONS.md. |
| 03 | Engine v2 | done | todo | pass (2026-09-26) | dd00852, 7d4ecb4, 5d7b3e9, 527f41c, 9732b79, 08c1206, 5dad4e7, 48cc4ec | Part 1 (Milestone 3A + WP-3.3/3.4), part 2 (WP-3.5/3.6) and part 3 (WP-3.7–3.9, the cutover) done. The v2 engine runs the server, desktop and SDK; the v1 engine, its routes and its tests are deleted; commands API + fork; ENGINE_LEVEL v2. Gate: typecheck 52/52; full suite = baseline failures only (cli 5, git 2, server 2; the core symlink failure left with SES); lint green with the invariants rule hard; no-legacy 99 bans, 0 hits. See STATUS "Phase 03 gate". |
| 03b | Stage conversation and run page | done | deferred to final review | pass (2026-09-26) | 0254811, 2529558, 63db929, f55692b | WP-3b.1–3b.4 done (IMPLEMENTATION FIRST: no new tests; the phase's server/web/store tests are deferred to the final pass). `StageConversationService` (send: 409 STAGE_BUSY mid-turn / INTERACTION_PENDING / queued between turns / amend a completed stage (PD-4, W-55) / retry a paused one; turn stop; gate answers in the chat's shapes; attachments), web compact chat + "…" menu + permission control + toasts, run-page streaming correctness and cost, mobile/TUI/CLI. Gate: typecheck 52/52; affected tests = baseline failures only (server 2, cli 5); lint green; no-legacy 100 bans, 0 hits, 0 legacy comments (baseline 5 → 0); no migration. 10 DEVIATIONS rows (P03b). Live E2E not run (advisory). |
| 04 | Lifecycle and invocation | done | deferred to final review | pass (2026-09-26) | 6fd9adc, a09e3d1, (docs) | WP-4.1–4.7 done (IMPLEMENTATION FIRST: one new test, the v58 chat-safety test; the plan's per-entry-point, idempotency, security, lifecycle and waitFor tests are deferred to the final pass). One lifecycle in `starting`/`finalizing` on MountService with journalled phases and `workflow_run.finalized`; `WorkflowInvocationService` + `POST /api/workflow-invocations`; every client (web, desktop, mobile, CLI, TUI, SDK, MCP remote, automations, scripts, testkit) migrated; orchestrator, preprocessor and the run-start routes deleted; v58 `invocation`. Gate: typecheck 51/51; affected tests = baseline failures only (server 1 CSP, cli 5, git 2; testkit T8 flaked under load, 4/4 alone); lint green; no-legacy 110 bans, 0 hits; migrations-lock 58; db-baseline v58; BaselineFreshDb pass; dbcopy-upgrade v52→v58 chat rows unchanged, drift 0. 16 P04 DEVIATIONS rows. Live E2E not run (advisory). |
| 05 | Control flow (5A + 5B) | todo | todo | todo | | |
| 06 | Agent integration and skill | todo | todo | todo | | |
| 07 | Economy, observability, UX | todo | todo | todo | | |
| 08 | Dynamic workflows | todo | todo | todo | | PD-21 default is **deferred**. WP-8.3 and WP-8.4 ship. WP-8.1, 8.2 and 8.8 are gated. WP-8.5–8.7 are backlog. |
| 09 | Release gate | todo | todo | todo | | |

## Phase 00: Baseline and safety net

| WP | Task | Status | Review | Commit | Notes |
|---|---|---|---|---|---|
| 0.1 | Branch, backup script, definition export | reviewed | reviewed | cbb9fb6, b176da5 | No new branch (single `wf/overhaul` branch, see DEVIATIONS). `scripts/workflow-backup.mjs` (`pnpm workflow:backup`, `--db` / `--out-root` / `--port`): refuses while :3100 listens, copies db/-wal/-shm, exports definitions + stages + edges from the COPY, prints counts. Run on a copy of the dev DB: 343 definitions exported = `COUNT(*)`. 2 unit tests. |
| 0.2 | `@generatorai/workflow-testkit` + characterisation tests T1–T8 | reviewed | reviewed | 1d54024 | Reuses `createCoreServices` + the Drizzle repos over an in-memory `migrateDB`. ScriptedFauxHarness: per-stage `Turn[]` (text, toolCalls, usage, error, delayMs, hang, on). Helpers: runWorkflow, snapshotRun, killAndRestart, route-equivalent commands. VirtualClock drives scripted delays only; no service accepts a clock (list in STATUS). Not wired: WorkspaceManager / worktrees / checkpoints, AdmissionController, browser and agent services, the StreamBroker event store. 30 characterisation tests with `KNOWN-BUG W-xx` markers + 8 unit/smoke tests; 38 pass in ~20 s. New finding: `recover()` re-drives runs before it rehydrates the allocator (W-32 race). |
| 0.3 | Isolated live E2E harness (`scripts/workflow-e2e/`) | reviewed | reviewed | 577c19e | server.mjs (start/stop/status; kills only its own PID tree), run.mjs, scenario.mts, pair.mts, lib/client.mts, lib/db.mjs, scenarios.json (phase 00: T1, T2, T3, T7, T10-small), specs. Live claude-agent (haiku): all 5 PASS. T1 first failed on a harness bug (transcripts truncated at 2000 chars); the one-line fix landed in a61ebbe by mistake. Faux path (`GENERATORAI_LOAD_TEST_FAUX_HARNESS`): all 5 PASS. No stray server left. |
| 0.4 | Dependencies and generators scaffold | reviewed | reviewed | 59a8de5 | fast-check (core, testkit); ajv ^8.20 + ajv-formats ^3 (core); zod-to-json-schema 3.25.2 (root, pinned to zod 3); `scripts/generate-workflow-spec.ts --check` no-op + `pnpm generate:workflow-spec`. |
| 0.5 | Golden session-composition snapshots | reviewed | reviewed | cf9e8c9, 3a2b94a | `packages/core/__tests__/session-golden/composeGolden.test.ts`: 7 file snapshots (a–g), LF-pinned. Drifts asserted as KNOWN-DRIFT: W-50 (systemPromptAppend / maxTurns reach the provider only on resume), W-51 (replace wipes the author's message; stage instructions land before the browser block), W-52 (team restrictions dropped). |
| 0.6 | Lint invariants (report-only) + `check-no-legacy` | reviewed | reviewed | 468dbb2 | `check:workflow-invariants` (report-only; baseline 21 direct stage-status writes) and `check:no-legacy` (+ `scripts/no-legacy.json`, empty), both in `pnpm lint`. 4 unit tests. |
| 0.6b | Migration lock + fresh-DB baseline | reviewed | reviewed | a61ebbe | Head verified: v54 = BASELINE_VERSION. `migrations.lock.json` (54 entries, CRLF-normalised) + `check:migrations-lock` in lint. `pnpm db:baseline [--check]` writes baseline.sql + baseline.generated.ts. `migrateDB` routes: empty → baseline; at/above baseline → versioned only; older → legacy. 7 tests (fixture built at v52, not copied; see DEVIATIONS). Dev-DB copy v52 → v54: chats preserved. |
| 0.8 | Run-worktree cleanup script | reviewed | reviewed | 23fca90 | `pnpm workflow:cleanup-runs [--dry-run]` through WorktreeService.removeWorktree. Branches deleted only if merged or never pushed; orphans listed; writes `cleanup.log` + `cleanup.json`. Real run tested on a git fixture (3 tests). Dry run on the dev-DB copy: 2 worktree rows, 33 run branches (7 deletable, 26 checked out in orphan worktrees), 72 orphan dirs. |
| 0.7 | Failure baseline recorded in STATUS.md | reviewed | reviewed | d772ee3 | §7 gate run once: typecheck 50/50; tests 27/31 packages green + 12 environmental baseline failures (symlink EPERM, CRLF autocrlf, POSIX paths, CLI TUI on Windows); lint green incl. 3 new checks; testkit 38/38; live E2E 5/5 (T1 after a judge fix); fresh-DB pass; Date.now() service list. |

## Phase 01: Spec v2, legacy purge, definition model

| WP | Task | Status | Review | Commit | Notes |
|---|---|---|---|---|---|
| 1.1 | Pure dead-code sweep | done | | 25fd04c | Every G3 §6 step-0 item deleted (web shims, write-only run-store state + its 3 stream ops, 12 dead hooks + useUpdateAutomation, ?legacy=1 → WorkflowRunPage, Stage actions button, orchestrator context route + activeContexts, resultValidator arg + W-62 listener, DAGScheduler views/aliases, IDAGScheduler/ISessionAllocator, ConfigResolver.resolve/resolveStageConfig, AppConfig.workflow, startPolling/importFromTemplate signatures, deprecated summary fields, CLI --session-mode (W-57), upload template). Hook-phase catalogue derived from HookDefinitionSchema (spec package takes over in 1.5). Timing setters kept as test seam (DEVIATIONS). 24 banned patterns. Tests: DAGScheduler rewritten onto reconcileRun (35 pass); hooks route test asserts schema-derived list; core/server/cli at baseline failures only. |
| 1.2 | Delete the v1 session/webhook stack | done | | 46667af | Deleted in G3 §6 step-1 order: webhooks routes + GET /hooks/sessions/:id/hooks, WebhookService (+ its condition grammar), SessionService (acp-entry → sessionRepo.getById), SessionStateMachine (core/shared/SDK), CreateSessionParams (McpServerConfig moved to its own file), Workflow/WorkflowStatus/WorkflowStateMachine, IWorkflowRepository/IWebhookRepository + Drizzle repos, verifyGitHubSignature/verifyWebhookToken, AppConfig.webhooks (+ SDK webhooks/maxConcurrentSessions), IPlatformClient session/workflow methods + Http/Mock clients + 11 web hooks, ConfigResolver (file gone; HookInterceptor keeps forwarding client lifecycle events). Also POST /orchestrator/from-template + createFromTemplate (web → POST /workflow-definitions/import, so web template imports no longer force autoCommit) and GET /orchestrator/system-workflows (web → GET /api/templates), with every caller: CLI webhook.* / hook.list / orchestrator.templates, client-core webhooks/sessionHooks/orchestrator.templates, admin views. Preprocessor condition grammar is replaced by Expression v2 in WP-1.5. DB tables stay until v55. |
| 1.3 | Required deps in `createCoreServices` | done | | 8d13095 | registerRepo, entryRepo, `sandbox` (an OrchestratorSandbox or null), workspaceManager, admissionController and `scmFlow` are required inputs; createCoreServices builds the DurableExecutionEngine and WorkflowPreprocessor itself. Constructors take their deps as required args (StageExecutionService, WorkflowRunService, HitlService (durableEngine required, in-memory fallback deleted), AutomationService, WorkflowPreprocessor, WorkflowOrchestrator); the DurableSleepService.onWake copy-paste went with PD-7. New `createRunSandbox` in core; the SDK wires SourceControlRegistry/ConfigService (secret store under artifactsDir)/FlowService, AdmissionController and the sandbox (`@generatorai/secrets` dep added). `createAllRepositories` includes register/entry repos. Test helper `createFakeWorkspaceManager()`; HITL tests use a real DurableExecutionEngine. Core/server/testkit at baseline. |
| 1.4 | Field and feature purges (PD decisions) | done | | 808be6b, d2b5a96, 3824099, fdbcb80, 78efffe, 8704c4a, 28072bd, 85dcbef, a22e68d, 488ca7b, e6a4cd7, ec124a8, 79171c8, 317e17e, a630bc5, efe44fd, 7cde9c4, 93481a8, ba36a91, 2f73188, 798028c, a425225, 109d59e, 8cf9caf | One commit per row, every layer (shared/core/db repos/server/SDK/client-core/cli-core/web/mobile/docs/doc site). Rows: gitRepositories + legacy clone path; createWorktrees; stage agentName + StageBuilder.agent(); interactive alias/coerceAgentMode; promptType/source/filePath/promptFile(); prompts[].attachments + Files tab; stage variables + editor; templateId/stage templates; iterationConfig + iterationIndex sort; selectedArtifacts/defaultAgentRef/definition skills+agents/scope; masterSessionId; PostProcessingStep.enabled (config required union); script permission vocabulary; HITL boolean approved (clients send outcome); plaintext webhook token + hashLegacyWebhookTokens; PD-8 automation legacy modes (one pipeline: dataSchema + IterationPlanner; CLI --data-schema/--iteration-mode/--default-dataset-*; web Iterations card); PD-7 durable sleep (service, status, events, wake route/UI/hooks/client/CLI/mobile); waitForCompletion + fire-and-forget branch + Wait checkbox; RunLogger/StreamLogger JSONL + stream-log.jsonl exclusions; unused override fields agentName/timeoutMs/contextFilter; test-only interrupt route (→ testkit `commands.interrupt`, T5 test); plan recording records the resolved harness (new optional `IAgentHarness.conversationHarness`, MultiHarness/HarnessProxy; T5 + MultiHarness tests). v1 sessions columns → v55. 69 banned patterns in `scripts/no-legacy.json`. **Not done here (by design):** DB drops and data conversion are v55 (WP-1.6, list below); `repo_path_target` stays until WP-1.7 (DEVIATIONS). **v55 must drop:** tables `workflows`, `webhook_registrations`, `webhook_deliveries` (first `chat_messages.workflow_id`, `artifacts.workflow_id`); `copilot_config*` on the 4 legacy tables; workflow_definitions `selected_artifacts`, `default_agent_ref` (+`idx_workflow_defs_agent_ref`), `skills`, `agents`, `scope` (+`idx_workflow_defs_scope`); stage_definitions `template_id`, `iteration_config`, `agent_name`, `variables`; workflow_runs `master_session_id`, `parent_stage_run_id` (+`idx_workflow_runs_parent_stage`); stage_runs `iteration_index`, `parent_stage_run_id` (+`idx_stage_runs_parent`), `wake_at`, `slept_since` (+`idx_stage_runs_wake_at`), dead `lease_owner`, and `sleeping` out of the status enum (convert any sleeping rows first); automations `webhook_token` (hash into `webhook_token_hash` first), `input_mode`, `loop_variable`, `loop_items`, `batch_data_format`, `batch_data`, `batch_columns`, `batch_column_mapping`, `data_source_config` (convert loop/batch to dataSchema + defaultDataset, or disable, first); sessions `repo_url`, `requires_codebase`, `workspace_path`, `triggered_by`. Defaults/constraints: drop `plan_documents.harness_type DEFAULT 'copilot'`; `chats.default_agent_mode` default → `auto` (+ rewrite `interactive` rows in chats/stage_definitions); `stage_edges.edge_type` NOT NULL; the `automation_execution_runs` FK fix. JSON in definitions converted to v2: prompts[] `waitForCompletion`/`source`/`filePath`/`attachments`, stage `promptType`, post-processing `enabled`, script permission vocabulary. Tests: core/web/mobile/shared/client-core/cli-core/server/db/sdk/testkit/changes/auth/agent-harness-providers run per row; baseline failures only. |
| 1.5 | `@generatorai/workflow-spec` final shapes | done | | 32c6d7c, ee51437, 6264483, 87a2b67 | New pure package (zod 3 only, browser-safe): SessionSpec, StageSpec v2 (agent kind; `stageBase` for the P05 kinds), EdgeSpec, WorkflowSpec (+`lifecycle`), WorkflowGraph `{formatVersion: 2}`, InvocationRequest + server-derived trigger union (P04 shape, `fork` not `retry`), RunCommand (P03 set), error taxonomy; Expression v2 (Pratt parser that never throws, type checker over variables + output JSON Schemas, bounded evaluator, function/filter registries, templates with bare-name sugar, `{{#if}}`, filters json/yaml/bullets, list literals); STAGE_RUN/WORKFLOW_RUN transition tables; `validateWorkflow` (6 layers, stable codes in `VALIDATION_CODES`, JSON pointers, rename/typo hints, engine gate with `ENGINE_LEVEL='v1'`); linear-time regex engine (RV-21); `resolveSessionSpec`; command-bearing registry + `commandFingerprint`; canonical `exportGraph`/`importGraph`; v2 builders (`/builders`); JSON Schema + FIELDS.md via `pnpm generate:workflow-spec` (`check:workflow-spec` in `pnpm lint`). Integrated now: DAGScheduler conditions and preprocessor `conditional` steps on Expression v2 (ConditionEvaluator and the preprocessor grammar deleted), regex rules on the safe engine, DAGValidator ordering on `analyzeGraph`, `/hooks/phases` from the spec catalogue (shared HOOK_PHASE_INFO deleted). Tests: 359 in the package (tables + fast-check: round trip, parser/template fuzz, regex vs RegExp, validator on generated graphs, describe/strict coverage, JSON Schema via ajv); core: DAGScheduler v2 conditions, preprocessor conditional/regex (new file), ResultValidator regex. Gate: typecheck 52/52; package 359/359; server hooks route 13/13; core/server/testkit/sdk runs showed baseline failures only; lint green; no-legacy 72 bans, 0 hits; `generate:workflow-spec --check` clean. Deviations: 4 rows (2026-09-25, WP-1.5). **Part C must wire (WP-1.6 to 1.9):** (1) v55 `convertLegacyStageToV2` must emit documents that pass `validateWorkflow(g, {engine: 'v1'})`: stage `condition` → `guard` (variables only) or edge `when` (anything reading `status`/`parentStatus` becomes `parent.status`); `AND/OR/NOT` → lower case; bare words → flag `needs_attention`; `resultValidation` + index-matched workflow validations → `output.rules`, with regex `value` → `pattern`, `custom_script` `value` (a command line) → literal `command` + `args`, and `llm_validation` dropped and logged; `expectedOutput` → `output.instructions`; pre/post-processing steps lose the outer `type` and `order` (sort by order first); stage hooks on workflow phases or on `on_client_*`/`on_permission` dropped and logged; `{{repo_path_<a>}}`/`{{repo_branch_<a>}}` → `{{run.codebases.<a>.path}}`/`.branch`; variables named after a reserved root (`run`, `item`, `map`, `loop`, ...) or `__*`/`repo_path_*` flagged. Until v55 runs, dev-DB definitions that use the old condition grammar evaluate as not holding (the stage is skipped). (2) WP-1.7 store/API: `validateWorkflow` on create/saveGraph/import/publish and `POST /workflow-definitions/validate` (returns `ValidationResult`); `parseGraph`/`exportGraph`/`importGraph` for storage and the canonical export; `commandFingerprint(old) !== commandFingerprint(new)` → require `admin:settings`; secret rules already run in the validator security layer; `GET .../schema` can serve `@generatorai/workflow-spec/workflow.schema.json`. (3) WP-1.7 v1 engine: guards and edge `when` via `conditionHolds` with scope `{variables, stages: {<key>: {status, output, summary}}, run: {id, name, codebases}, parent: {status}}` (`parent` in edge `when` only); prompts via `renderTemplate` (then delete `repo_path_target`); ResultValidator on `output.rules` (`rule.pattern`/`flags` → `compileSafeRegex`); session via `resolveSessionSpec(workflow.session, stage.session, runOverrides)`; then delete core `domain/dag/DAGValidator.ts` (build the DAG with `analyzeGraph` over keys), `packages/shared/src/config/{WorkflowDefinitionSchemas,WorkflowTemplate,WorkflowScriptSchema}.ts` and `packages/shared/src/builders/*`; WorkflowScriptLoader → `@generatorai/workflow-spec/builders` `buildWithHandlers()` + `HookExecutor.registerFunctionHandler` for the returned handlers; drop the shared stored-hook schema so its phases match the catalogue. (4) WP-1.8 builder: `validateWorkflow(graph, {engine: ENGINE_LEVEL})`, issues map by `path`/`stageKey`; the engine-gated controls are exactly `engineIssues` in `validate/capability.ts`; live expression check with `checkExpression`; help from `EXPRESSION_GRAMMAR`/`grammarFunctions()`. (5) WP-1.9: FIELDS.md is generated (done); the feature docs remain. |
| 1.6 | Migration v55 `workflow_definitions_v2` | done | | 1d67d0f, 9c61ec6 | JS migration step (`Migration.run` + `lockFiles` hashed into the v55 lock entry; DEVIATIONS) with frozen spec schemas and converter under `migrations/v55/`. FKs off, explicit child deletes; stage-run sessions/messages deleted by `owner_type`. Every item of the P01 drop list (TRACKER 1.4, notes/P01-v55-drop-list.md): legacy tables and columns, sessions v1 columns, automations legacy modes (loop/batch → dataSchema + defaultDataset, script mode disabled, webhook tokens hashed), install drift (the P00 drift allowlist is now empty). Definitions → v2 documents (keys, key edges, guard/edge `when` in Expression v2, `output.rules`, `{{run.codebases.<a>.path}}`, notes in `needs_attention` + `_migration_log`), published at version 1. New `workflow_definition_versions`; runs pin `definition_version_id`; stage runs carry `stage_key`; run history purged. Precondition: a completed `workflow:cleanup-runs` for the DB, or `GENERATORAI_V55_SKIP_RUN_CLEANUP=1`. Baseline regenerated (`BASELINE_VERSION` 55). Tests: `migration55.test.ts` (3: v54 DB with chats/messages/stage sessions → chat rows, messages and chat sessions unchanged; conversion; idempotent re-run), BaselineFreshDb (drift allowlist empty, history purged), replay tests rebuilt on v54 DBs; db 134 pass. `pnpm workflow:dbcopy-upgrade` on the dev-DB copy: v52 → v55 in 3.6 s, chat rows UNCHANGED (STATUS). |
| 1.7 | Definition service, store and API | done | | 1d67d0f | `IWorkflowDefinitionStore` + `SqliteWorkflowDefinitionStore` (whole-graph save in one IMMEDIATE transaction, optimistic revision, immutable published/test versions deduped by content hash, archive, explicit deletes); `WorkflowDefinitionService` rewritten (createFromSpec = the one materializer, saveGraph, publish, resolveVersionForRun — drafts need testRun —, import/importTemplate, delete-or-archive, command-field scope via `commandFingerprint`); `RunDefinitionReader` (version cache); the v1 engine reads the pinned version by stage key (buildDAG on `analyzeGraph`, guards/edge `when` via `conditionHolds`, templates via `renderTemplate` with `run.codebases`, `output.rules` with Ajv/safe regex/command+args, `resolveSessionSpec`). Routes rewritten (list/create/validate/import/get/PUT graph/publish/versions/export/delete; 409 `REVISION_CONFLICT` with `current`, 422 `WORKFLOW_INVALID` with `issues`, 403 on command fields). Templates `{id, category, graph}` validated at boot; the 3 scripts on the v2 builders. Callers: shared (now depends on workflow-spec), SDK (create publishes by default), client-core (definitions API, typed scripts API, `ApiError.body`, `needsOrchestratedStart`), CLI (cli-core, apps/cli, tui-kit: read-modify-write with one 409 retry, `--test-run`, keys), mobile, mcp-server (`testRun`), server tests + OpenAPI, doc-site generators, testkit. Deleted: per-row repos/ports, DAGValidator, resolveStageHooks, withTransaction, shared v1 schemas/builders, `repo_path_target`. 6 bans added (78). Tests: testkit 43/43 (T1–T8 on v2 specs; T6 flipped to PASS with round trip, malformed imports, draft → test run → publish → run, pinned version W-13, conflict, command scope, archive); core rewritten suites (DAGScheduler 24, WorkflowDefinitionService 16, WorkflowRunService 35, SES/replay/timeout, ResultValidator, golden unchanged); server 5 new route tests on the real service; cli-core workflow-stage (409 retry). |
| 1.8 | Builder on v2 (behaviour) | done | | 28f9c5e | Store keyed by stage key (D-1), delete-and-reconnect (D-2), key rename, clearing deletes (D-4), positions, one-step auto-layout (D-28); whole-graph save with expectedRevision, no reload while saving/dirty (D-3), 409 dialog, 422 inline; live `validateWorkflow` with node badges and field issues, save blocked on errors, engine-gated controls disabled with a tooltip; v2 stage panel + edge panel with live expression parse; Variables tab stable ids/raw options/name checks (D-10, D-11); draft/published badges, publish, test run, canonical export/import, archive on delete; run page reads the pinned version. Tests: builder store 16, builder page (no reload while dirty, 409 reload), variable labels; web 636/636. Not done: budget/compensate are disabled placeholders (engine-gated); `deriveRunView` prompt preview keeps the old `{{name}}` substitution (display only). |
| 1.9 | Docs | done | | ebb4e82 | FIELDS.md generated (`check:workflow-spec` clean); `feature-workflows.md` and `feature-stages.md` rewritten (graph save, keys, versions, draft/publish, import/export, Expression v2, typed codebase scope, removed-field tables); the fictional SDK examples replaced with the real builder API (feature-workflows, sdk README, usage-sdk); AGENTS.md, feature-templates-scripts and other feature docs updated; doc site regenerated (configuration, examples, reference, modules). |

## Phase 02: SessionComposer

| WP | Task | Status | Review | Commit | Notes |
|---|---|---|---|---|---|
| 2.0 | Golden snapshots first | done |  | (no code) | P00 golden suite re-run before any change: 7/7 green at wf-phase-01-done (0c02b1c parent). |
| 2.1 | Pure helpers | done |  | 0c02b1c | `session/cfg.ts`: appendSystemBlock, appendTools, unionList (+ systemContent). Replaced the inline system-block / tool-list / exclusion-list copies in CMS (create and resume) and SES. No behaviour change: golden 7/7 byte-identical. |
| 2.2 | PlatformToolBinder | done |  | a8324be | `session/PlatformToolBinder.ts` (browser, computer, widgets, SCM hint, custom, orchestrator, hooks, widget digest; owner-aware: chat or stage). CMS create and resume call it (chat snapshots byte-identical; `registerComputerUseSkill`/`selectCustomTools` moved into the binder). `SessionComposerDeps` (session/types.ts) is the shared deps object; `ChatManagementServiceExtensions` extends it. Orchestrator: `OrchestratorService.registerStageParent` + `parentView` (chat or stage parent; workers stay chats with `parentChatId` = stage run id; no nudge for a stage parent, digests via check_*); tool set takes `parentId`. W-54: `SessionHookRegistry` + `sessionHookBridgeFactory`; the server composition root assigns `buildHookBridge`; ExtensionManager commits `ai.registerHook` function hooks into it (function hook ctx gains `event`). RV-26: functions in spawn params cross the agent-host IPC as `{__hostCallback}` markers (`serializeHostCallbacks`/`hydrateHostCallbacks`), host stubs send `callback_invoke`, the gateway answers `callback_result`; `send_turn` now carries the turn options; agent-host protocol v2. Tests: session/sessionHooks.test.ts (a chat hook fires and denies; no bridge when empty), agent-host binderContract.test.ts (tools, permission gate, hook bridge, turn options with the host ON and OFF). Stage wiring of the binder lands with the SES switch in WP-2.8. |
| 2.3 | resolveMcp for both owners | done |  | 2ea6ce0 | `session/resolveMcp.ts`: `mergeMcpServers({agent, explicit})` (explicit last, so it beats an agent default for both owners) then the hub keyed by owner (`chat:<id>`/conversation, or definition id/run id); dropped servers become `mcp_dropped` composer warnings (emitted as `harness.session_info`); when the hub leaves nothing, the agent's unresolved map is removed so no `secretref:` pointer reaches a provider. CMS create and resume use it (golden b gains `mcpServers: {}`: the create path's rule now applies on resume too). Stages call it through the composer in WP-2.8, where the v1 engine's `secretref:` MCP gate is lifted. Tests: session/resolveMcp.test.ts (stage secretref headers resolved with the stage's hub scope; hub-disabled and missing-secret servers dropped with warnings and no pointer left; explicit beats agent). |
| 2.4 | Agent projection, instructions, skills | done |  | 52df8ae, 7666200 | `session/agentProjection.ts`: `applyAgentProjection` (scope chat|stage, resolver layers base/binding, snapshot pass-through; team with tools/disallowedTools/reasoningEffort/maxTurns/permissionMode, W-52; stage + missing/disabled agent → ComposeError agent_not_found/agent_disabled, C-12), `applyExplicitSpec` (one precedence rule for create and resume: lists/instructions/provider from the merged spec, effort/context tier/turn cap from the binding-site layer only; systemPromptAppend and maxTurns now reach the provider on create, W-50), `appendAgentInstructions` (last; `replace` drops only the replaceable base, W-51), `deliverSkills` per provider level (claude-agent: `AgentStagingService.ensurePlugin` → `<root>/.generatorai/plugin/{.claude-plugin/plugin.json, skills/<name>/…}`, `plugins: [{type:'local',path}]`, `skills: ['generatorai:<name>']`, no skillDirectories; Copilot/Codex: directories, Codex warns skills_process_global (C-19: the app-server has no per-thread roots); opencode/acp: skills_unsupported). `session/chatSpec.ts` maps a chat row/request to `SessionSpec` (PD-20). `IAgentHarness.resolveProvider` (MultiHarness) tells the composer which provider a model routes to; `CreateConversationParams.plugins` → Claude `Options.plugins` (settingSources stays []). CMS create and resume use the module; the resume path now follows the create order. Golden: W-50 flipped (a/c/d/e gain systemPromptAppend + maxTurns, e's maxTurns 12→7 because the explicit value beats the agent; b equals a key for key, asserted). Stage use of the module (and deletion of `resolveStageAgent`, W-51/52 golden flips) lands with the SES switch in WP-2.8. Tests: session/agentProjection.test.ts (instructions last, replace keeps platform blocks, team fields, C-12, precedence, skills per provider incl. plugin root), providers workspace-mounts (plugins passed, settingSources [], repo hooks not loaded). |
| 2.5 | Workspace exposure for stages | done |  | 1fb1574 | `session/workspaceExposure.ts`: `workspaceExposure(manager, workspace, {workingDirectory?})` (the workspace's exposure; a pinned cwd — a run's primary worktree, which the v1 engine chooses — moves every other mount and the managed root into additionalDirectories and re-renders the `[Workspace]` hint for it), `applyWorkspaceExposure(cfg, exposure)` (cwd, extra dirs, env; returns the hint), `runWorkspace(manager, run)` (the run's workspace by id or owner; missing → ComposeError `workspace_missing`, no `process.cwd()` fallback, B-5). CMS uses it (chat snapshots unchanged). Stages get exposure through the composer in WP-2.8, where SES's `process.cwd()` fallbacks are deleted. Tests: session/workspaceExposure.test.ts. |
| 2.6 | GatePort, TurnContextRegistry, tool-policy wrapper | done |  | 47806a2 | `session/gates.ts`: `GatePort` (permission, question, planReview, recordPlan), `TurnContextRegistry` keyed by conversation id (+ takeSequence), `withAgentToolPolicy` (agent tool groups refuse before any gate, chats included, W-53). `session/modeConfig.ts`: `applyModeConfig` installs the three gate callbacks (they look up the turn in flight and call the owner's port), `planModeInstructions` (spec value or default), the auto-mode plan-recording block and `record_plan`; `planPromptPrefix` only for providers without a native plan gate (P7). `session/StageGatePort.ts`: HITL-backed permission (`tool_permission`), question (`question`, new), plan review (`plan_review`, new, with a PlanDocument and revisions) and `record_plan` (T7); the semaphore permit is released while parked; events `stage.permission.*`, `stage.question.*`, `stage.plan.*` (chat shapes + stageRunId; AgentEvent union, eventClass, client-core router alias the chat cards). CMS: `ChatGatePort` wraps the existing chat handlers (now methods taking the turn), `chatModeConfig` replaces `applyPlanModeConfig`, the per-chat `turnContexts` map is gone (registry by conversation id, shared via the constructor); `agentModePolicy.TurnContext` deleted. Chat snapshots unchanged. Stage gates are installed by the composer in WP-2.8 (bind failure → error + idle there). Tests: session/gates.test.ts (chat enforces tool groups; workers get no gates; a shared conversation files the gate against the stage in flight; question and plan-review verdicts given after a restart reach the relaunched stage via a second HitlService on the same DB — a gate-level restart rather than testkit killAndRestart, whose v1 engine skips the interrupted mutating turn; record_plan files a stage plan). |
| 2.7 | PermissionModeSource, unattended defaults | done |  | 8cfe62a | `session/permissionSource.ts`: `PermissionModeSource` (chat row | run), `runPermissionMode` (run row → stage `session.permissionMode` → workflow's → trigger (`__triggerPermissionMode` engine state) → undefined = deployment posture; never bypass), `runPermissionSource` (reads the run row every turn), `turnOptionsFrom(agentMode, source)`, `checkPermissionGating` (PD-17: `none` refuses default/plan with PERMISSION_GATING_UNSUPPORTED; `exec_and_patch` warns on default/acceptEdits; plan allowed). `DEFAULT_WORKFLOW_RUN_PERMISSION_MODE` deleted; `getPermissionMode` returns the effective mode (run → workflow → trigger → posture) and `setPermissionMode` reports the previous effective one. `CreateWorkflowRunParams.permissionMode` (run row) + `triggerPermissionMode`; retries keep the ancestor's mode; the scripts route passes the profile mode at create. `startRun` refuses a run a stage provider cannot hold (`PermissionGatingUnsupportedError`, 400; provider via `IAgentHarness.resolveProvider`). PD-18: `Automation.permissionMode` required (schema `AutomationPermissionModeSchema`, repo mapping, v56 column), passed to each run as the trigger mode; webhook + bypass needs admin:settings on create/update; `PATCH /workflow-runs/:id/permission-mode` to bypass needs admin:settings (shared `routes/permissionScope.ts`, chat routes use it); CLI `automation create --permission-mode` (default acceptEdits) / `update --permission-mode`; web automation editor has the field (default acceptEdits) and the detail page shows it. Per-turn options for stage follow-up/review turns and the W-19 composer-level test land with the SES switch (WP-2.8). Tests: session/permission.test.ts (layers, per-turn re-read, posture not bypass, automation mode required, PD-17 table, run refused at start). |
| 2.8 | Composer assembly and caller switch | done |  | 8f3dbe3 | `session/SessionComposer.ts`: one `compose()` for chat and stage in the canonical order (base → resume id → workspace exposure → agent projection → explicit spec → workspace hint → binder: browser, computer, widgets, SCM hint, MCP, custom, orchestrator, hooks → skills → mode config → agent instructions last); returns params, projection, provider, binding key (`session/bindingKey.ts`), warnings, `turnOptions(agentMode)` (permission re-read every turn) and `preparePrompt`. Resolves `session.provider` `secretref:` keys (unresolved → ComposeError `secret_unresolved`) and MCP `secretref:` values; the v1 engine gate that refused both is lifted (workflow-spec capability.ts; validate test flipped). Stage-only: PD-17 check at compose; `host_tools_unsupported` / `host_tools_start_only` warnings (C-11); computer use opt-in and refused on bypass (PD-5); orchestrator from the spec/role, stage registered as parent. CMS create and resume call it (chat golden a–e byte-identical apart from the W-50 fields already flipped). SES: constructor takes the composer (browserService param gone); `resolveStageAgent`, `buildPermissionHandler`, `resolveStageTurnOptions`, `setAgentServices`, `setBrowserService`, the stage session-config block, the plan-mode prompt prefix and every `process.cwd()` fallback are deleted (R-1); `definitions/sessionSpec.ts` deleted (`resolverLayer` moved to agentProjection). `SessionAllocator.allocateSession(run, stage, mode, build)`: the composer runs for create, resume (resume passes the composed params and provider session id) and single-mode attach; its auto-approve fallback is gone. Bind failure (R9): `harness.error` + `harness.idle` on the stage stream, stage failed with the ComposeError code. Stage agent projection frozen on the stage run's durable artifact channel (`agent-snapshot`) and reused by retries/restarts. Follow-up and in-session retry turns now get the stage's turn options (they sent none). Golden: W-51 and W-52 flipped (f: instructions last, team fields; g: stage browser + workspace exposure) and snapshots f/g regenerated. Tests: session/composer.test.ts (W-19 at composer level: claude-agent and codex on `default` park on the first gated tool, codex carries the PD-17 warning, opencode refused, acceptEdits passes edits, mode re-read per turn; computer use opt-in/bypass; custom tools without the authoring pair; host_tools warning; BYOK secret resolved/refused; agent_not_found); SES/allocator/W46/DurableReplay tests moved to the builder API (W46 null-config fallback test deleted with the fallback). Testkit T8 W-32 characterisation: the race no longer reproduces in the testkit (the stage composes before allocating, so the rehydrate wins); the test now pins that, the race stays in code until P03 WP-3.6. Core 1752 pass / 1 baseline symlink EPERM; testkit 43/43; workflow-spec 364/364; turbo typecheck 52/52. |
| 2.9 | TurnRecorder and provider-session resume | done |  | ad35d22 | `session/TurnRecorder.ts`: the one per-turn accumulator (tokens, reasoning, tool calls merged on callId with success/fileOp/parentId, text segments, provider anchor, errors) with the sequence shared with the gate cards; `take({partial, fallbackContent})` builds the assistant message once, `snapshot`/`restore` feed the W22 journal. Replaces the CMS `sendPrompt` accumulators and all three SES listeners (executeStage, retryInSession, sendStageFollowUp now share `listenStageTurns` + `persistStageTurn`; the eight per-turn reset blocks became `recorder.begin` inside `sendTurn`). Stage messages gain turnId, agentMode, textSegments, per-call success/fileOp/sequence, providerAnchor, and live checkpoints (`scheduleLiveCapture` on tool_complete with the stage run as provenance). The recorder observes before the event is forwarded, so the record is complete when the provider call returns (the stage fallback write used to race the listener). RV-10: `ChatMessage.complete` (assistant rows; repo writes `complete ?? !metadata.partial`, reads it back for assistant rows); true on the final turn event, false for a stopped/paused turn. F-3b: `session/providerSession.ts` `rememberProviderSession` shared by CMS and `SessionAllocator.rememberProviderSession`, called after every stage turn (resume already passes the handle, WP-2.8). Tests: session/turnRecorder.test.ts (complete vs partial, calls settled on stop, once per turn, anchor, snapshot/restore; allocator records the provider handle), SES test "records a stage turn the way a chat turn is recorded", migration56.test.ts repo round-trip of `complete`. Core 1756 pass / 1 baseline EPERM; db 137; testkit 43; turbo typecheck 52/52. |
| 2.10 | Migration v56 `session_parity` | done |  | 1cb1991 | v56 `session_parity` (SQL only, no rebuilds, chat-safe): `chat_messages.complete INTEGER NOT NULL DEFAULT 1` with `complete = 0` backfilled where `json_valid(metadata) AND json_extract(metadata,'$.partial') = 1`; `automations.permission_mode TEXT NOT NULL DEFAULT 'acceptEdits'` (existing rows get acceptEdits, PD-18). schema.ts updated, lock entry added, baseline regenerated (BASELINE_VERSION 56). Found and fixed: with the baseline at 56 a v55 database took the legacy route, whose bootstrap fails on a post-v55 schema (`no such column "order"`) — the dev DB would have failed its next upgrade. `chooseMigrationRoute` now sends every database at or above `VERSIONED_ONLY_FROM = 55` down the versioned route (DEVIATIONS). Tests: migration56.test.ts (a v55 DB with chats, a partial message, a stage session and automations: chat rows unchanged by hash, `complete` backfilled, automations get acceptEdits, FK check clean); migration55.test.ts now stops at 55; db 136 pass / 4 skip. `pnpm workflow:dbcopy-upgrade` on C:/gaiwf/dbcopy: v52 → v56 via legacy in 2.7 s, chat rows UNCHANGED (362 chats, 392 chat sessions, 841 messages), drift 0. |
| 2.11 | UI: SessionSpecEditor | done |  | a220bb3 | `apps/web/src/components/session/SessionSpecEditor.tsx`: one editor for a `SessionSpec` with host-chosen `sections` (runtime: model, provider, reasoning effort, context tier; mode: default agent mode, permission mode; agent: picker + overrides + effective capabilities; skills; MCP; platform: browser, and for workflow/stage computer use and widgets; warnings). Patch semantics (`applySessionPatch`). Inline capability warnings from `sessionCapabilityWarnings` in @generatorai/shared, which now holds `PROVIDER_CAPABILITY_LEVELS` (moved from core so the web reads the composer's table; core re-exports it); a stage is judged over its workflow session, and a model with no provider set is judged by the provider it routes to. `SkillSelector`, `McpServerSelector`, `AgentBindingSection` moved to components/session and take `{session, onChange, projectId}` instead of a stage. Hosts: stage panel (Model section = runtime+mode+warnings; agent sub-tab, Skills, MCP sections; new Platform Tools section), workflow General tab (the full editor: adds workflow-level provider, context tier, permission, agent overrides, skills, MCP and platform toggles, D-31), CreateChatDialog (runtime+mode+agent+warnings, skills and MCP under advanced; `chatParamsFromSession` maps the session onto the chat create fields, PD-20; orchestrator and browser pickers unchanged). Automation permission field done in WP-2.7. Existing primitives only (ModelPicker, Select, ToggleSwitch, AgentPicker, AgentOverridesEditor). Tests: SessionSpecEditor.test.tsx (PD-17 refusal by routed provider, stage judged over workflow, platform toggles and chat scope, chat mapping); SkillSelector test moved to the session props. Web 640/640; typecheck; check:tokens ok. |

## Phase 03: Engine v2

| WP | Task | Status | Review | Commit | Notes |
|---|---|---|---|---|---|
| 3.1 | CAS repositories and RunStore | done | | 527f41c | `stage_runs`/`workflow_runs` `transition(id, from[], to, opts)` on `DrizzleStageRunRepository`/`DrizzleWorkflowRunRepository` (impl `db/repositories/engineCas.ts`): synchronous CAS checked against `STAGE_RUN_TRANSITIONS`/`WORKFLOW_RUN_TRANSITIONS` (dev/test throw `IllegalTransitionError`, production rejects + `process.emitWarning`); entering starting/running REQUIRES a lease (stamps lease, heartbeat, progress, `started_at` in the same UPDATE; `'none'` for containers); leaving the attempt states clears the lease; `renewLease`, `markProgress`, `claimOwnership` (owner_epoch + 1 when unowned/own/expired), `getInstance`/`getRunRow`. New sync repos (`EngineRepositories.ts`): `StageAttemptRepository`, `RunSessionRepository`, `WorkflowTimerRepository` (arm replaces the live timer of the same run/instance/kind; fire is a CAS), `WorkflowOutboxRepository` (markDispatched idempotent by (run, run_seq)), `SchedulerJournalRepository`. `RunStore.loadRunState` + `RunStore.apply(runId, ownerEpoch, decisions, {now, random?, message?, stateHash?})`: one `IMMEDIATE` better-sqlite3 transaction, fenced first on owner_epoch (every run statement carries it; every instance statement is limited to the run), decisions applied in emission order, any lost CAS aborts the whole batch (`conflict`), jitter drawn here, outbox `run_seq`, journal row; returns effects/timers/outbox seqs. Ports: core `domain/ports/IRunStore.ts`. `check-workflow-invariants` allows `engineCas.ts`/`RunStore.ts` (the CAS implementation). Tests: `db/src/__tests__/RunStore.test.ts` (12: illegal pair, lease stamp/clear, claim-vs-cancel race, version pin, ownership fencing, decide→apply round trip to completion with outbox/journal/timers, stale epoch fenced with nothing written, lost CAS rejects the batch, jitter/minDelay/timer replacement/fire-once, outbox redelivery, attempts, run sessions). |
| 3.2 | Migration v57 `workflow_engine_v2` | done | | dd00852 | `migrations/v57_workflow_engine_v2.ts` + frozen DDL `migrations/v57/ddl.ts` (both lock files). Precondition as v55 (`GENERATORAI_V57_SKIP_RUN_CLEANUP`). Purge with explicit child deletes through `v57_purge_sessions`/`v57_purge_workspaces` (RV-1; owner_type/scope filters; chats' sessions excluded). Drops `automation_execution_runs`, `stage_session_maps`, `session_allocations`, `stage_runs`, `workflow_runs`; creates `workflow_runs` (G5 §6.2 + trigger, invocation_id, idempotency_key (partial unique), parent_run_id, parent_stage_run_id, root_run_id, depth, run/stage overrides, codebase_selection, system_vars, workspace_id, permission_mode NOT NULL, owner_id/owner_epoch/owner_expires_at, run_seq), `stage_runs` (instance_path unique per run, scope_id, iteration/item index, loop_state, expansion, leases, amended_at; status CHECK = v2 states), `stage_attempts` (+agent_snapshot, judge, structured_output), `run_sessions`, `workflow_timers`, `workflow_outbox`, `scheduler_journal`, `engine_lock` (CHECK id = 1), `automation_execution_runs` again; `chat_messages.turn_role` added, no CHECK, no backfill. Timestamps ms. Idempotent replay. schema.ts, baseline (v57), lock. v1 adapted minimally (DEVIATIONS, 5 rows): the v1 repos map `queued`↔`ready`, retryCount↔current_attempt, steps in `usage.v1Steps`, the run permission mode in `run_overrides`, allocator state in `run_sessions`; testkit v1 adapter's raw query; T3's W-39 marker now asserts `stage_attempts` is empty. Tests: `migration57.test.ts` (chats/sessions/messages hash-identical, worker chat kept, stage messages removed exactly, definitions untouched, FK check clean, replay no-op; CHECKs and a free turn role on a fresh DB); the migration56 test pinned to 56. `pnpm workflow:dbcopy-upgrade` on C:/gaiwf/dbcopy: v52 → v57 via legacy in 7.3 s, chat rows UNCHANGED (362 chats, 392 chat sessions, 841 messages), 343 definitions (19 invalid, all with notes), drift 0. |
| 3.3 | Pure scheduler core (`decide()`) | done | | 5d7b3e9 | `domain/workflow-graph/compile.ts` (`compile(graph)` → `CompiledWorkflow`: engine defaults applied once, guards/`when` pre-parsed with parse errors kept, edges indexed in key order, `handlesFailure` = `on: failure` or the flag; `RunDefinitionReader` stays for v1 until the cutover) and `domain/scheduler/` (`types.ts`, `ids.ts` UUIDv5, `readiness.ts`, `terminal.ts`, `decide.ts`): `decide(graph, state, msg, now)`; join all/any/n_of_m per predecessor; guards after readiness (eval error → `condition_error`); skip reasons + cause; `cancelRemaining` over exclusive predecessors (`cancelled_loser`); `computeScopeOutcome` (W-29); precedence: 0 drop → retry (transient / safe interrupted / repairable + restartOnExhausted; retryOn; maxAttempts counts failed + interrupted attempts; totalMs; budgets; an unclassified error ≤ 1 retry) → an unsafe interruption pauses → route → onExhausted; success only from `validating` (F-5); stage and run budgets (stage: abort after the desired state; run: drain pause `budget_exhausted`, wall-clock timer); `maxParallel` + `sessionGroup` admission (a `ready` instance with a live attempt is admitted; `status_reason` carries the next attempt mode); run states waiting/finalizing/cancelling; commands (run pause drain/interrupt, resume, cancel; instance pause/resume/retry/skip/fail/cancel/approve, expectedVersion, desired state before abort); timers retry, queue_timeout, pause_ttl (PD-2, 72 h, unattended), run_budget_wall_clock; `finalize` carries the LIFO compensation list. Tests (78): `core/__tests__/scheduler/readiness.test.ts` (join table, scope outcome table), `decide.test.ts` (lifecycle, S1/S3/S4/S5/T2, precedence table, budgets, concurrency, commands, timers, determinism); the harness checks G5 §7.2 invariants 1 and 3 on every step. **Not done (speed rule, DEVIATIONS):** the fast-check model test over all ten §7.2 invariants and the §7.3 replay fixtures; containers (P05). |
| 3.4 | Error taxonomy | done | | 7d4ecb4 | core `domain/errors/StageError.ts`: `ClassifiedError`, `StageError` (engine-raised, carries its classification), `classifyStageError` (StageError, classified values, `HarnessError` duck-typed, `ComposeError`, `AdmissionTimeoutError` → queue_timeout, `StageRejectedError` → rejected_by_human, HTTP status, errno, message patterns; unknown → transient/transport flagged `unclassified`), `errorClassOf`, `codeForHttpStatus`; codes and classes from the spec's `STAGE_ERROR_CODE_CLASS`. `agent-harness-providers/src/errors.ts`: `HarnessError {code, retryAfterMs?, providerCode, unclassified}` + `toHarnessError(provider, raw)` (Claude SDK assistant-error enum and result subtypes, Anthropic API error types, Codex `codexErrorInfo` incl. nested HTTP status, HTTP/JSON-RPC/errno). Tests: provider recorded fixtures (37, each also run through the core classifier), core classifier table (23). Wiring the providers' failure paths through `toHarnessError` is the StageExecutor's harness boundary (WP-3.5). |
| 3.5 | StageExecutor | done | | 9732b79 | `services/engine/StageExecutor.ts` (carved from SES; additive): owns the in-attempt CASes (claim `ready → starting` with lease `bootId:stageRunId:attemptNo`, `starting → running`, `running → validating`, `validating → running` for repairs and the approval gate, `running ↔ awaiting_input` with a live frame), reports one `attempt_settled`; status re-check after every turn, and a stop wins over an aborted turn that resolves (F-1, O-9, B-3). Sessions via the P02 composer; `run_sessions` key `instance:<path>@<epoch>` or `group:<g>`, config-hash rebind through `resumeConversation` (RV-20); provider session id on resume; a recap prefix for a conversation that lost its history. Turn journal (`ITurnJournal`, db `StageTurnJournal` over `registers`): intent + user message in one tx, settlement + assistant message `complete = 1` in one tx (RV-10); ops `a{epoch}/prompt|repair|summary|review/…`, epoch = the latest non-resume attempt; an interrupted op is re-sent after a notice. Context fenced `<generatorai:stage-context trust="untrusted">` in the first prompt, no context-ack turn (O-3), no output-retry turn (F-10), a summary turn only for text stages whose successor reads `summary`. `OutputExtractor.ts`: strategies native → `submit_output` (ajv at call time) → final JSON block by provider levels (RV-9; Codex resume falls back with a warning), ajv 2020 + hard rules on the latest prompt-turn answer (F-5/F-6/F-7/B-11), repair turns while `repair_count < maxRepairs`, then `output_schema`/`validation_rule` (repairable). Attempt deadline over every turn with HITL time excluded, idle watchdog, queue timeout cancelled at the claim, lease renewal 20 s, progress writes 10 s (B-7, B-8, W-15). Usage ticks feed budgets. `pre_run` variables on an attempt copy (W-41). The approval gate validates first, then parks (lease cleared, admission slot handed back); revisions are journalled, persisted and validated (F-2, W-46); rounds capped; a verdict given after a restart rides the resume attempt. Checkpoint "before" in `starting`, skipped for read-only stages; a restart restores the attempt-1 checkpoint. `toHarnessError` injected at the harness boundary. Also: `SendPromptOptions.outputSchema`/`ConversationResponse.structuredOutput` (claude-agent `outputFormat`, `error_max_structured_output_retries` → repairable; Codex `turn/start.outputSchema`), `StageGatePort.park`, shared `outputRules.ts` (ResultValidator uses it), `IEngineStore` ports + `createEngineStores`, `ChatMessage.turnRole` mapped. Tests: core `engine/outputExtractor.test.ts` (10), db `EngineStores.test.ts` (5: journal atomicity and in-flight, lock, forced claim fences). Not done: native-output unit/live tests, the Codex `dynamicTools` re-send check (DEVIATIONS). |
| 3.6 | Actor, supervisor, ownership, timers, leases, outbox, recovery | done | | 08c1206 | `services/engine/`: `RunActor` (serial mailbox, `decide` → `RunStore.apply(epoch)` → dispatch, 3 CAS retries, fenced → retire, `onDecide` tap), `RunSupervisor` (engine lock at boot, 30 s stale, 10 s renew, `EngineLockedError`; forced run claims `owner_epoch + 1`; `startRun`, `command(RunCommand)`, `post`, `recover`, `idle`; commands to a terminal run refused as `invalid_state`), `TimerService` (fire CAS, chained delays, owned runs), `LeaseReaper` (15 s, owned runs, safe replay from the journal), `OutboxDispatcher` (awaited, ordered per run, retried; RV-5 terminal kinds), `EffectsDispatcher` (the admission controller as the only gate, W-66; abort drops a queued launch; `deliver_input` with no frame → settle + re-post the approval), `DefaultRunLifecycle` (prepare: workspace + `on_run_start`; finalize: LIFO compensation, `onFailure`/`onExit`, run hooks, session release B-15, workspace completion). Recovery: `ready` relaunched; attempt states → `interrupted/process_restart_unsafe` (a never-replay turn in flight → paused) or `interrupted/lease_expired` with safe replay (the resume replays settled turns); `awaiting_input` frames settled `aborted` (the verdict starts a resume attempt); timers re-armed; lost prepare/finalize re-dispatched; outbox drained (W-16, W-32, B-10, B-20). The commands route and the client switch move to part 3 (DEVIATIONS). Testkit: `createV2Adapter`/`v2Adapter` (a crash leaves the lock; the restart waits for it to go stale), `Turn.submit`, `turn_role` classification, snapshot attempts/turnRole/complete/statusReason. Tests (19 testkit + 8 core): v2 T1 (2), T3 (4), T5 (5), T8 (3, incl. two engines on one DB refused), output contract (3), replay fixtures (2; `UPDATE_FIXTURES=1` writes `core/__tests__/fixtures/scheduler/*.jsonl`), core `engine/runActor.test.ts` (5), `scheduler/replay.test.ts` (3). Runs: core 1880 pass / 1 baseline EPERM, db 156, providers 741, testkit 64/64. |
| 3.7 | Switch, delete v1, flip | done | | 5dad4e7 | `RunSupervisor` built by `createCoreServices` (engine stores, `toHarnessError`, outbox publisher; admission is the one gate) and started/stopped by the server, desktop and SDK (`EngineLockedError` → no engine, commands 503). `WorkflowRunService` = facade (create without pending rows, start with the PD-17 check, command, fork, delete, permission mode, `failSetup`). Commands route `POST /workflow-runs/:id/commands` (approve = `exec:agent`, others also `write:workflows`); per-action routes and `pending-interrupts` deleted; web/mobile/CLI/TUI/SDK/client-core switched; route policy. Deleted: DAGScheduler, SES, SessionAllocator (+ port/repo), StartupRecoveryService (→ `runBootHousekeeping`), ResultValidator, v1 state machines, stage Semaphore, `DEFAULT_RETRY_POLICY`, the v57 repo mappings, the HitlService waiter (→ instances + run commands), v1 events (`stage_run.queued/step_*`, `workflow_run.retried`). Gaps closed: prepare runs the PD-17 check + project worktrees; `awaiting_input → paused` (`frame_lost`) for in-turn gates lost to a restart; stage overrides in `decide()`/executor; turn journal released at finalize; outbox events published to the run scope awaited (bridge skips `runSeq` events). Shared `StageRun`/`WorkflowRun` = v2 read models. `ENGINE_LEVEL='v2'`, builder `EngineGated` removed. Invariants rule hard (BASELINE 0), HAZ-1 → `StageExecutor.turn()`, 9 P03 bans. Testkit: v1 adapter + `current-engine/` deleted, v2 default, T2/T4/T7 ported, KNOWN-BUGs flipped. Tests: decide frame_lost + skip override, T8 lost tool permission, T4 (W-17/F-5/W-02), T7 (W-48/W-49), server commands/fork routes, streamScopes outbox skip. |
| 3.8 | forkRun | done | | 5dad4e7 | Landed in the cutover commit (R-2: `/retry` becomes fork in the same commit; DEVIATIONS). `forkRun(source, {rerunFrom, definition: pinned\|latest, variablesOverride, workspace: fresh\|reuse\|restore_checkpoint, idempotencyKey, start})`: terminal source only (409), memoized = not downstream of `rerunFrom` (default: every instance that did not complete), copied with `copied_from_stage_run_id` and never re-validated (B-6); `latest` re-runs changed stages; permission mode, stage overrides, project, codebase selection and `trigger {kind: fork}` carried (W-59); `workflow_run.forked`. `ForkRunRequestSchema` in the spec (FIELDS.md). Web "Retry failed" (+ "Re-run from this stage"), CLI `run retry [--from]`, SDK `workflows.fork`, mobile retry. Tests: testkit `fork.test.ts` (memoization, lineage, 409 on live, idempotency). |
| 3.9 | Docs | done | | 48cc4ec | `FIELDS.md` regenerated (engine level v2, the new state row, the fork request); `.github/docs/feature-workflow-runs.md` rewritten (tables, states, attempts, precedence, gates, ownership/recovery, commands, fork, overrides, CLI/SDK); doc site: architecture execution/data docs, feature workflow-runs, generated route reference and examples; SDK API-STABILITY durability note. |

## Phase 03b: Stage conversation and run page

| WP | Task | Status | Review | Commit | Notes |
|---|---|---|---|---|---|
| 3b.1 | StageConversationService | done | | 0254811 | core `services/engine/StageConversationService.ts` + `StageConversationError.ts`; routes `POST /workflow-runs/:id/instances/:iid/messages` (multipart prompt/attachments[]/mode; 202 `{outcome: queued\|amending\|retrying}`; 409 STAGE_BUSY mid-turn (PD-3) / INTERACTION_PENDING / STAGE_NOT_STARTED / STAGE_NOT_CONVERSABLE), `…/turn/cancel {force}` (the turn settles with what it produced; the stage continues; force destroys + re-binds the conversation), `…/interactions/:id/{permission\|answer\|plan}` (chat body shapes → `approve`; deny/decline = changes_requested; route policy `exec:agent`), `…/attachments/:artifactId`. Executor: frame operator queue drained after each prompt turn and before success (closed flag), per-turn abort, `amend()` (resumes the instance's session key, re-opens a released session, operator turn + contract/repairs, `IStageRunCas.amend` → output_text/output_data/amended_at, `stage_run.amended` / `amend_failed`, re-releases for a terminal run), attachments as stage artifacts (`JournalMessage.attachments`). Retry command + `promptOverride`/`attachmentIds`/`agentMode` (carried via interrupt_data → attempt overrides; claim clears interrupt_data). Review batch → send (completion review keeps approve changes_requested; delivered only on success). PD-9: `followUpPrompt` fallback deleted, banned. Client-core/web platform/SDK calls; router events (operator message, dropped, turn cancelled, amended). Tests deferred to final pass (send 409, amend, queue, cancel, attachments, gates, review batch). |
| 3b.2 | Web: stage as a compact chat | done | | 63db929 | `StageComposer` (shared ChatInput, `manageStream=false`, attachments, agent mode, two-phase Stop → turn cancel) for the focused stage; StreamPanel gets `widgets` + `streamKey="stageRun:<id>"` + gate callbacks through the interaction routes; InlineHitlControls only for the completion review (busy-disabled, no follow-up); stage "…" menu (pause, resume, retry resume/restart, skip as completed, cancel with confirm, copy output, open in inspector, re-run from here = fork when terminal); RunHeaderBar permission-mode control (W-65) + cancel confirm; every control toasts refusals (D-13, mutation meta); context gauge chip next to UsageChip; "Amended" chip; operator bubbles; review batch to the focused stage. Committed with 3b.3 (DEVIATIONS). Playwright tests deferred to final pass. |
| 3b.3 | Run-page streaming correctness and cost | done | | 63db929 | `stage_run.*` events carry stageKey/instancePath/version (decide + executor; replay fixtures regenerated); store: interruptData copied (D-19), unknown instances inserted, polls merged per instance by version (D-21b), one focus field + `suggestStageRun` for SSE (D-20; graph/timeline/page share it); `protectStream` for every stage of the mounted run (D-21); clock only in the header, StageView cache per stage, stable callbacks (D-24); workspace polled only while live; scratchpad route/hook/types deleted (nothing writes it in v2; DEVIATIONS); Inspector Files = focused stage's checkpoint changes (D-22). Store tests deferred to final pass (the timer tests were removed with the store clock). |
| 3b.4 | Mobile and TUI | done | | 2529558 | Mobile: chat `Composer` (compact) on the stage screen with the stage send/stop, stage gates as the chat's permission/question/plan cards (StageGateCard) on the run and stage screens, completion review stays ApprovalCard (Approve-with-note removed, PD-9), operator bubbles, 409 toasts. TUI: stage gate events reuse the chat's banner/overlays (a/x) posting to the stage routes. CLI: `run stage send <run> <stage> <text\|-> [--mode] [--attach]`, `run stage stop [--force]`; 409 codes surfaced. Tests: mobile 1098, cli-core 873, tui-kit 99, cli 318 + the 5 baseline failures. 2 DEVIATIONS rows. |

## Phase 04: One lifecycle, one invocation path

| WP | Task | Status | Review | Commit | Notes |
|---|---|---|---|---|---|
| 4.1 | Lifecycle into the engine (MountService) | done | | 6fd9adc | core `services/engine/lifecycle/` (`prepare.ts`, `finalize.ts`, `steps.ts` = the ported preprocessor, `runUploads.ts` = the one upload writer, `hooks.ts`); `DefaultRunLifecycle` journals each phase in `system_vars.lifecycle['prepare/<phase>'\|'finalize/<phase>']` (resume at the phase; hooks once per phase, C-17) and emits `workflow_run.phase_started/completed/failed`; `decide()` emits `workflow_run.finalized` after every terminal event (prepare failure included). Prepare: workspace (+`on_run_start`) → worktrees = the run's mounts through `MountService.plan/stage/prepare/ready` (request codebases → `lifecycle.codebaseAliases` → CODEBASE_REQUIRED; never all, W-22; generated mount otherwise; `pre_clone`/`post_clone`) → uploads → projectConfigs → preprocess (`run_script` cwd = working dir, `pwsh -NoProfile -NonInteractive -Command` on Windows / `sh -c`; `clone_repo` https/ssh only + `--`) → sandbox (fail closed unless `lifecycle.sandbox: optional`, new spec field; `on_all_stages_scheduled`). Finalize: compensate (journalled per outcome) → hooks (onFailure/onExit + outcome hooks) → postProcess (completed runs only; commit/push/PR through the flow on `system_vars.codebases`, PR base = the mounted baseRef; a failed `failOnError` step fails the run) → release (sessions, turn journal, sandbox, workspace; worktrees never removed, C-7). A cancel during finalizing supersedes the finalize in flight (skips post-processing, runs compensation). System values moved out of variables into `system_vars`/columns: working dir, codebases, skill dirs, custom agents, sandbox, trigger permission ceiling (W-06 by construction; the executor reads them; hook attachments under the workspace root, C-8; workspaceId a column, C-6). Deleted: `WorkflowOrchestrator`, `WorkflowPreprocessor`, `routes/orchestrator.ts` (workspace reads → `GET /workflow-runs/:id/workspace*` over the mounts), `createRunWorktrees`/`setupProjectWorktrees`, `failSetup`, `stripExecutionContext`, the orchestration/worktree/pre-post started-completed events, the dead run-scope helpers. Tests: none new (IMPLEMENTATION FIRST; phase lifecycle tests deferred to the final pass); existing preprocessor/SCM tests moved to `LifecycleSteps`; decide/replay fixtures/RunStore/golden tests updated (goldens byte-identical, R-10). |
| 4.2 | `WorkflowInvocationService` | done | | 6fd9adc | core `services/workflow-invocation/`: `invoke` (parse → scopes → idempotency claim with request hash, 24 h, derived keys chat/stage/auto → resolve definition/script (materialized once per content hash, tag `script-hash:`)/fork → validate → `createRun` with typed columns (trigger, invocationId, lineage, runOverrides, stageOverrides, codebaseSelection, system_vars, budget) → start → `workflow_run.invoked`), `plan` (no writes), `waitFor` (subscribes before its read; finalized / approval / timeout / abort; W-63), `digest`, `stageUploads`/`sweepUploads`. `validateInvocation` (typing moved out of the run facade, `__*`, stage keys, codebase aliases of the project, requiresCodebase, model catalog, PD-17 gating, permission ceiling with PERMISSION_ESCALATION, depth ≤ 3 + recursion, child-run budget, uploads), `planInvocation` (layers, override/static-guard skips, model/provider/agent, codebases, phases, post-processing, warnings), `InvocationError` envelope codes. `IdempotencyService` lifted from the automation route. Automations: one invoke per iteration/attempt (automation trigger with iterationIndex, the automation mode as ceiling = `system_vars.triggerPermissionMode`), `waitFor(finalized)`; `waitForRunCompletion` deleted. `IterationPlanner` refuses `__*` and undeclared fields and no longer injects `__iteration_*` (C-3). Spec: `RunProfileSchema` replaces `ScriptRunProfileSchema`; `InvocationPlan`/`InvocationResult`/`RunDigest`/error codes/phase lists. Tests: none new; automation/permission/scheduled-run/IterationPlanner/testkit tests adapted. Tests deferred to final pass: one per entry point (C-1), idempotency, security, lifecycle crash/resume, waitFor races. |
| 4.3 | One route and one client method | done | | 6fd9adc | server `routes/workflowInvocations.ts`: `POST /api/workflow-invocations` (JSON or multipart `request` + files), `/uploads`, `/plan`, `GET /:runId/digest?wait=`; trigger derived from the principal (`user` / `external_agent` for service accounts and `client: mcp`); one `{error:{code,message,issues}}` envelope; route policy `/workflow-invocations` = `exec:agent` + `read:workflows` (W-60, PD-6), in-service scope checks (script → write:workflows; bypass off loopback / in_place → admin:settings). Deleted: `POST /workflow-runs`, `/:id/start`, `/:id/fork`, `/workflow-scripts/:id/run`, `/orchestrator/*`; OpenAPI updated. client-core `workflows.invoke/plan/uploads/digest`, `runs.workspace*`; deleted `runs.create/start/fork`, `orchestrator.*`, `scripts.run`, `stageOverrides.ts`/`workflowStart.ts` (→ `runStart.ts`: stage/codebase drafts, idempotency key). `IPlatformClient.invokeWorkflow/planWorkflowInvocation`. Migration v58 `invocation`: `idempotency_keys.request_hash`, `invocation_uploads`, `auth_devices` rebuilt with the `mcp` platform (frozen DDL, lock, baseline v58); `migration58.test.ts` (chat rows + devices unchanged by hash, FK check clean) — the one new test. dbcopy-upgrade: v52 → v58, chat rows UNCHANGED, drift 0. |
| 4.4 | Client migration: CLI, TUI, SDK, MCP, scripts | done | | 6fd9adc | CLI `run start` = one `workflows.invoke` (`--var`, `--stage-var`, `--skip`, `--stage-model`, `--model`, `--effort`, `--codebase alias[@ref][:in_place]`, `--project`, `--permission-mode`, `--name`, `--run-timeout <min>` (DEVIATIONS), `--profile`, `--skill/agent/prompt-file`, `--test-run`, `--idempotency-key`); `--watch`/`run watch` long-poll the digest to `finalized`; `run plan`; `run retry` = fork; `script run` = invoke script (W-56 both bugs); profiles parsed with `RunProfileSchema` (C-10), the CLI profile type deleted; `orchestrator` group deleted; `device invite --platform mcp` via the API with a `generatorai-mcp pair` hint; CLI docs + surface snapshot regenerated. TUI: `run.start` inherits the flags and shows the plan in a confirm overlay before starting. SDK: `workflows.invoke/run/plan/fork/waitFor/digest`, `stream()` replays the run scope from the event log then live until `finalized`; `orchestrate`/`createRun` deleted; `ScriptFacade.run` = script invocation; the SDK wires MountService + the lifecycle platform. MCP: remote mode only (`remote.ts`: `mcp` device pairing, vault namespace `mcp/<serverId>`, client-core over the authenticated runtime), bin `generatorai-mcp pair|serve`, `generatorai_run_workflow` = invoke; embedded mode + in-process tool registry deleted (W-58). Auth: `mcp` platform + `DEFAULT_MCP_SCOPES`; Devices page icon (revocation as any device). Testkit + `scripts/workflow-e2e` start runs through the invocation. Tests: cli-core 873, cli 318 + 5 baseline, sdk 8, mcp-server 7, testkit 47. |
| 4.5 | Client migration: web, desktop, mobile | done | | 6fd9adc | Web: `useInvokeWorkflow`/`usePlanWorkflowInvocation`; `RunDialog` replaces `VariableInputModal` (variables; Stages always visible with skip / stage variables / per-stage model, D-15; Run options model / effort / permission mode (deployment default, effective mode from the plan, W-65) / name / stop after N min; codebases checkboxes + branch from `codebaseDrafts`; uploads as files; Plan preview; per-open idempotency key; inline errors, W-27); definition page, builder (test run), script pages and run page (retry / re-run from = fork) all start through it; `HttpPlatformClient` run starts delegate to client-core (G3 5.13); orchestrated branches, `downloadRunFile`/`getRunDiff` dead code deleted. Desktop: embeds the web. Mobile `StartRunSheet`: one invoke, uploads staged first (race gone), Advanced = run name / model / effort / permission mode (the PermissionModeSheet options) / codebases (sent) / stages / files; `scripts/[id]` = script invocation; retry = fork; runs by `run.projectId`; `runStart` feature gate = exec:agent + read:workflows (PD-6); `mcp` device labels. Tests: web 635, mobile 1097. Playwright/E2E deferred to final pass. |
| 4.6 | Legacy removal checklist | done | | a09e3d1 | 9 P04 bans (110 total, 0 hits): orchestrator/preprocessor identifiers, run worktree helpers, client start helpers, `waitForRunCompletion`, old profile schemas, every `__*` system variable name, `/orchestrator/*` and the run-start routes, `runs.create/start/retry/fork`, the replaced events, `orchestratorConfig` (client paths). P01 `parentStageRunId` ban split (DEVIATIONS). Acceptance grep `orchestrator/runs\|startOrchestratedRun\|__workingDirectory\|__stageOverrides` over packages/apps (non-migration source): 0. |
| 4.7 | Docs | done | | (this commit) | `docs/workflow-overhaul/generated/INVOCATION.md` generated from the schemas (`renderInvocationMarkdown`, part of `generate:workflow-spec --check`); `.github/docs/feature-workflow-runs.md` §0 (one invocation, one lifecycle), tables, fork, profiles, CLI/SDK/MCP; `feature-workflows.md`, `apps.md`, `packages.md`; CLI docs (`pnpm cli:docs`, in 6fd9adc); doc site: run-start section, regenerated route/config/example references (the run-profile example on `RunProfileSchema`). |

## Phase 05: Control flow as generic DAG building blocks

| WP | Task | Status | Review | Commit | Notes |
|---|---|---|---|---|---|
| 5A.1 | Spec, grammar delta, validator codes, presets | todo | | | |
| 5A.2 | `check` runtime (Windows launch fix) | todo | | | |
| 5A.3 | Loop engine | todo | | | |
| 5A.4 | Migration v59 | todo | | | |
| 5A.5 | Loop UI + run commands (web, mobile, TUI, CLI) | todo | | | |
| 5A.6 | Judge rule | todo | | | |
| 5B.1 | Map engine and mounts | todo | | | |
| 5B.2 | Sub-workflow + WorkflowApprovalService | todo | | | |
| 5B.3 | Wait (approval/event/timer, callback tokens) | todo | | | |
| 5B.4 | Fork, compensation and join UI | todo | | | |
| 5B.5 | Expression editor (CodeMirror 6) | todo | | | |
| 5B.6 | Remaining templates and docs | todo | | | |

## Phase 06: Agent integration and authoring skill

| WP | Task | Status | Review | Commit | Notes |
|---|---|---|---|---|---|
| 6.1 | Workflow tool set | todo | | | |
| 6.2 | Chat ↔ run bridge and UI | todo | | | |
| 6.3 | Orchestrator integration | todo | | | |
| 6.4 | Stages invoking workflows | todo | | | |
| 6.5 | `WorkflowAuthoringService` and endpoints | todo | | | |
| 6.6 | Skill bundle generation | todo | | | |
| 6.7 | Channels | todo | | | |
| 6.8 | MCP tools and resources (no prompts) | todo | | | |
| 6.9 | Docs | todo | | | |

## Phase 07: Economy, flow keys, tracing, UX

| WP | Task | Status | Review | Commit | Notes |
|---|---|---|---|---|---|
| 7.1 | Turn economy | todo | | | |
| 7.2 | Flow keys | todo | | | |
| 7.3 | Budgets UI and cost | todo | | | |
| 7.4 | Tracing alignment | todo | | | |
| 7.5 | Write amplification measurement | todo | | | |
| 7.6 | UX gaps | todo | | | |
| 7.7 | Docs | todo | | | |

## Phase 08: Dynamic workflows (PD-21 gated)

| WP | Task | Status | Review | Commit | Notes |
|---|---|---|---|---|---|
| 8.1 | Spec for `kind: 'dynamic'` | deferred | | | Gated by PD-21 |
| 8.2 | Script runtime (QuickJS) | deferred | | | Gated by PD-21 |
| 8.3 | Judge-panel template | todo | | | |
| 8.4 | Plan-then-execute expansion | todo | | | |
| 8.5 | Run diff | deferred | | | Backlog in the plan |
| 8.6 | Stage test with pinned data | deferred | | | Backlog in the plan |
| 8.7 | Opt-in stage cache | deferred | | | Backlog in the plan |
| 8.8 | Builder/run UI for dynamic workflows | deferred | | | Gated by PD-21 |

## Phase 09: Release gate

| WP | Task | Status | Review | Commit | Notes |
|---|---|---|---|---|---|
| 9.1 | Full live E2E suite | todo | | | |
| 9.2 | Security review | todo | | | |
| 9.3 | Docs from source | todo | | | |
| 9.4 | Register reconciliation | todo | | | |
| 9.5 | Performance and resource budgets | todo | | | |

## Review log per phase

Each phase review appends its findings and their dispositions here.

### Phase 00 review (independent reviewer, 2026-09-24): pass-after-fixes

All 24 findings are fixed in the review-fix commit (`adb0961`). Tests re-run: `pnpm lint`, the db tests, the testkit tests, the golden test and `pnpm test:scripts`. A faux E2E run was repeated and a dry run on the dev-DB copy re-done.

| # | Sev | Finding | Disposition |
|---|---|---|---|
| R1 | blocker | cleanup-runs deleted chat-owned `generatorai/run-*` branches by prefix | **Fixed.** A branch is deleted only when its 8-char id is a workflow run / automation id (`workflow_runs`, `automation_executions`, `worktrees` rows of those types, run workspaces) AND no chat owns it (chats, chat workspaces, other worktree rows); unknown owners are kept. Fixture test with a chat-owned, unmerged, unpushed `run-*` branch. Dev-DB-copy dry run: `run-924d2a71-Backend` → "keep (chat-owned)"; 9 chat-owned kept. |
| R2 | major | worktrees found only through `mode='worktree'` mount rows | **Fixed.** Also scans `<root>/source/*` (`parentCloneOf`, now bare-clone aware) and matches `git worktree list --porcelain` paths under run workspace roots. Fixture: a run worktree with no mount row is removed. |
| R3 | minor | dry run counted prunable worktrees as "checked out" | **Fixed.** Porcelain `prunable` entries are parsed and ignored; fixture asserts the prediction. |
| R4 | minor | "pushed" read `refs/remotes`, which bare product clones do not have | **Fixed.** `git ls-remote --heads <remote>` per remote; an unreachable remote keeps every unmerged branch. Bare-clone fixture with a plain `git push`. |
| R5 | minor | dry run opened the DB read-write | **Fixed.** Dry run opens better-sqlite3 `readonly` and builds no writing services (fixture asserts the DB file hash is unchanged); `git status --porcelain` summary logged before every forced removal. |
| R6 | major | golden serialised `tool.parameters` (field is `parametersSchema`) | **Fixed.** Every ToolDefinition field except `handler` is serialised (`parametersSchema`, `skipPermission`, `requiredPermissions`, …); snapshots regenerated. |
| R7 | major | snapshots held Windows-only `<workDir>\\run` | **Fixed.** Paths under `<workDir>` normalised to `/`; snapshots regenerated. |
| R8 | major | no-legacy lacked the §7.6 legacy-comment rule | **Fixed.** Comment rule over the workflow module (path regexes in `no-legacy.json`); report-only with a per-phase switch (`phase` / `failFromPhase`) and a growth ratchet at the recorded baseline of **64**. |
| R9 | minor | invariant rule missed `batchUpdateStatus`, `stageRunRepo!.`, patch variables | **Fixed.** Widened; new baseline **24** (was 21), with a growth ratchet. |
| R10 | major | v52 fixture was synthetic | **Fixed.** `fixtures/schema-v52-dev.sql` is the real dev DB's schema (schema-only dump, no user rows) + synthetic rows; it upgrades via the legacy path with chats intact; drift vs fresh is pinned to an explicit 8-entry allowlist for P01 to empty. DEVIATIONS row updated. |
| R11 | minor | schema check was one-way | **Fixed.** Reverse diff (extra physical columns, defaults, indexes, FKs) with an explicit 20-entry allowlist; it surfaced 5 indexes `schema.ts` declares that no migration creates. |
| R12 | minor | two processes on one empty DB could collide | **Fixed.** Baseline runs under `BEGIN IMMEDIATE` and re-chooses the route inside the transaction (`applyBaselineIfEmpty`); versioned migrations also `BEGIN IMMEDIATE` and skip a version already recorded. Race test added. |
| R13 | nit | `db:baseline --check` not in lint; FTS shadow tables | **Fixed.** `check:db-baseline` in `pnpm lint`; `dumpSchema` skips virtual-table shadow tables. |
| R14 | minor | `zod-to-json-schema` resolved against zod 4 | **Fixed.** The monorepo's schemas are zod 3 (3.25.76); zod 4's `z.toJSONSchema()` only accepts zod 4 schemas, so the dependency is needed. The root now pins `zod` ^3.25.76, so the lock resolves `3.25.2(zod@3.25.76)`; rationale recorded in `generate-workflow-spec.ts`. |
| R15 | minor | backup port probe was IPv4-only; timeout meant "free" | **Fixed.** Probes `127.0.0.1` and `::1`; a timeout counts as listening. IPv6 test added. |
| R16 | nit | backup folders had second precision | **Fixed.** `YYYYMMDD-HHMMSS-mmm` plus a non-recursive `mkdir` that never reuses a folder (`createBackupDir`); test added. |
| R17 | minor | `server.mjs stop` could kill any `tsx src/index.ts` | **Fixed.** `verifyOwnServer`: the command line must contain THIS worktree's tsx CLI; the OS creation time must match the pid-file record; the process tree must own the :3111 listener. `E2E_PORT=3100` is refused. Tests added. |
| R18 | nit | `SERVER_URL` could aim the harness at :3100; creds piled up | **Fixed.** The URL comes from `server.mjs` (3100 refused); the creds file is deleted at the end of every run; the 5 old creds files were removed (5 existed, not 6). |
| R19 | minor | two KNOWN-BUG asserts could not flip | **Fixed.** T3 asserts earlier attempts' errors are absent and no `stage_attempts` table exists (W-39). T2 asserts the generic skip reason and that `validateDefinition` raises nothing about `stages.R` (W-31). |
| R20 | minor | testkit welded to the v1 engine | **Fixed.** `EngineAdapter` interface (`types.ts`): startRun, command (data `RunCommand`s), snapshot, isTerminal, killAndRestart, resolveStage, classifyTurn. All v1 knowledge (route logic, v1 tables, private heartbeats, `stage-<id>-<ts>`) is in `adapters/v1.ts`. The snapshot keys instances by `instancePath` (v1: the stage name). Turns are classified from the persisted message metadata (`turnRole` first), with text as a fallback. |
| R21 | minor | wall-clock table incomplete | **Fixed.** HookExecutor, WorkflowPreprocessor, WorkflowScriptLoader, WorkflowOrchestrator added in STATUS.md. |
| R22 | minor | no repeatable real-DB upgrade check | **Fixed.** `pnpm workflow:dbcopy-upgrade` (copy → `migrateDB` → counts + sha256 of chat/session/message rows before/after + drift); recorded in STATUS.md §5b. Result: chat rows unchanged. |
| R23 | nit | `scripts/__tests__` not run by turbo/CI | **Fixed.** Root task `//#test:scripts` (`pnpm test:scripts`) that `turbo test` depends on, so CI's `pnpm turbo test` runs it. |
| R24 | nit | stray `console.log`; dead `void services`; judge fix inside `a61ebbe` | **Fixed / noted.** The log became an assertion; `void services` went with the engine split. The E2E judge fix (transcript truncation 2000 → 20 000 chars) landed in `a61ebbe` (WP-0.6b) by mistake; history not rewritten. |

### Phase 01 review (independent reviewer, 2026-09-25): conditional pass → fixed

All 8 findings are fixed in `fix(workflow): P01 review fixes R1-R8`. Re-run: typecheck (52/52), `pnpm lint` (green; no-legacy 79 bans, 0 hits; migrations lock; db baseline; workflow-spec generated files), and the affected packages' tests: workflow-spec 365, db 135 (+4 skipped), core 1719 (+1 failing: the symlink EPERM baseline), auth 45, shared 326, client-core 294, cli-core 872, workflow-testkit 43, sdk 8, mcp-server 18, server 540 (the 2 baseline failures only), mobile 1098, web 636. `pnpm workflow:dbcopy-upgrade` re-run on the dev-DB copy.

| # | Sev | Finding | Disposition |
|---|---|---|---|
| R1 | major (security) | `createRun` copied caller variables unfiltered: `repo_path_*` (so `run.codebases`) and `__workingDirectory` / `__skillDirectories` / `__customAgents` / `__stageOverrides` were caller-controllable | **Fixed.** `createRun` refuses any variable matching `FORBIDDEN_VARIABLE_NAME_PATTERN` (`ValidationError`, 400); engine state comes only from typed params (`projectId`, new `triggeredBy`, new `stageOverrides`), and retry carries its ancestor's engine state through a private path. `UserVariablesSchema` refuses the same names; `POST /workflow-runs` validates with it and takes a typed `stageOverrides`; the scripts route, automations, orchestrator, CLI, web and mobile send typed overrides (`encodeStageOverrides` has one shape; `STAGE_OVERRIDES_VARIABLE` deleted). The scheduled-run "drop inherited context" code is gone (callers can no longer seed it). Route test: reserved names → 400, typed overrides pass through. |
| R2 | major | 42 of 343 migrated versions failed `validateWorkflow` (engine v1), 24 with `needs_attention` NULL, so they silently could not run | **Fixed.** The frozen copy under `migrations/v55/spec/` now includes the validator (`validate/`, `expr/`, `regex/`, `util/`, `state/`, `commandBearing.ts`, schemas under `schemas/`), and the converter runs it: undeclared bare `{{x}}` → optional string variable (logged); defaults coerced to the declared type (or dropped with a note); a schema on a text output dropped (v1 never applied it); every remaining error pushed into `attention`. Test: a definition with all four cases converts, and the only remaining error is noted. `dbcopy-upgrade` now fails when an invalid definition has no notes; dev copy: 343 migrated, **19** still invalid (`expr-not-boolean` 16, `expr-scope-unavailable` 2, `expr-stage-not-upstream` 1), **0** without notes; chat rows unchanged; drift 0. v55 re-locked, baseline regenerated. |
| R3 | major | Hook `env`/`url`/`headers`/`bodyTemplate`/`cwd` validated as v2 templates but rendered with `{{\w+}}` (HTTP) or not at all (script env) | **Fixed.** `HookExecutor` renders them with the spec `renderTemplate` over `HookContext.templateScope` (the stage/run services pass `templateScope(run, variables, stageRuns)`; a fallback derives `variables`/`run` from the hook variables); the old interpolator is deleted; dry run reports render errors. Test: script env/cwd and HTTP url/headers/body render `variables`, bare names, `run.*` and `stages.*`. |
| R4 | major | Runs used `sessionMode: 'auto'`, which shared one conversation across a linear graph; v2 default is `sessionReuse: 'fresh'` | **Fixed.** Runs are created `per-stage` and the graph-shape resolution is deleted. T8's parallel-stage workaround reverted; T7 now asserts one conversation per stage, each with its own model; the core session-mode tests replaced. |
| R5 | major | `retry.maxDelayMs/jitter/retryOn/mode/restoreCheckpointOnRestart`, `approval.allowChanges/maxRounds`, `workflow.outputs` accepted with no v1 reader | **Fixed.** `engineIssues` rejects them on v1 when they differ from the schema default (`outputs` when present); the builder renders max delay, jitter, allow-changes and rounds disabled with the upgrade tooltip. Gate table extended; arbitraries keep the defaults. Note for P03: the v1 retry starts each attempt on a fresh conversation, which is closer to `mode: 'restart'` than the default `resume`. |
| R6 | major | `session.provider.apiKey` and MCP `secretref:` env/header values reached the harness verbatim | **Fixed (gated).** The v1 capability gate rejects `session.provider` (workflow and stage) and any `secretref:` value in MCP server env/headers until P02 resolves them. Gate-table rows added. |
| R7 | major | `{prefix:'/webhooks', public:true}` survived the deleted route (fails open) | **Fixed.** Entry removed; test asserts `/webhooks/*` is not public; banned in `no-legacy.json`. (There was no existing test expectation to delete.) |
| R8 | minor | Definitions list read one page (client-core / mobile) or `limit=500` once (web) | **Fixed.** Both follow `nextCursor` (page size 200). |

**Open items for later phases** (from the part C report; not fixed here):
- The SDK creates `WorkflowScriptLoader` without `{ enabled }`, so `ai.scripts` never loads scripts (P06, SDK surface).
- A graph with zero stages passes `validateWorkflow`; confirm this is intended for empty builder drafts (P03 compiler or P06 authoring).
- Core's `MockStageRunRepository` no longer satisfies `IStageRunRepository` (`interrupt`, `resumeFromInterrupt`, `findAwaitingInputByRun`, `updateStatus` return type); core `__tests__` are not type-checked (P03, when the engine tests are rewritten).

### Phase 02 review (independent reviewer, 2026-09-25): not ready (1 blocker, 8 majors) → fixed

All 9 findings are fixed in `fix(workflow): P02 review fixes R1-R9`. Re-run: `pnpm turbo typecheck` (52/52), `pnpm lint` (green; no-legacy 90 bans, 0 hits; migrations lock v56; db baseline v56; workflow-spec generated files), and the affected packages' tests: core 1761 (+1 failing: the symlink EPERM baseline), workflow-testkit 45, workflow-spec 364, server 541 (the 2 baseline failures only), web 640.

| # | Sev | Finding | Disposition |
|---|---|---|---|
| R1 | blocker (security) | A BYOK `provider.apiKey` could name any vault secret and send it to a caller-chosen `baseUrl` (chats with `write:chats`, workflows with `write:workflows`) | **Fixed.** Provider keys resolve only from the `provider` namespace (`secretref:provider/<name>`): the composer refuses any other namespace before reading (`secret_unresolved`), `McpCredentialVault.resolveRef` returns null for any other namespace, and the validator requires the prefix. Setting or changing a chat's `harnessConfig.provider` needs `admin:settings` (create and PATCH; `canSetSessionProvider`). `session.provider` (workflow and stage) is in the command-bearing registry (`kind: 'provider'`), so adding or changing it needs `admin:settings` through the fingerprint. Test: composer refuses `secretref:mcp-github/token` without reading the store. |
| R2 | major | A resume from the frozen agent snapshot turned credentialed MCP `env`/`headers` into `'<redacted>'`, which injection then spread into `{"0":"<",...}` | **Fixed.** `redactProjection` keeps the maps and their `secretref:` pointers and masks only literal values (`MCP_REDACTED_VALUE`). On a snapshot resume a masked server's config is re-derived from the live agent by server id (the frozen server set stands); one the agent no longer has stays masked and injection drops it with an `mcp_dropped` warning. Golden case (h): a credentialed agent MCP server (pointer + literal, and pointer only) resumed from the redacted snapshot gets the same resolved servers as the live compose. |
| R3 | major | `ensurePlugin` wrote one plugin root per workspace and deleted other skills; parallel stages and orchestrator workers share the workspace | **Fixed.** One plugin root per conversation (`.generatorai/plugin/<conversationId>/`); a conversation only ever prunes its own. Test: a second conversation on the same workspace leaves the first one's skills in place. |
| R4 | major | After a restart stages resumed with no tools, gates or MCP: `resumeStage` put a bare config in memory first; single-mode `attach` composed and discarded; `stageTurn` never composed | **Fixed.** The bare resume in `resumeStage` is deleted (the allocator's composed resume runs). `attach` resumes a conversation that is not live WITH the composed config (a live one keeps its config: no rebind, RV-20). `stageTurn` composes and resumes when the conversation is gone; the stage config is built in one place (`composeStage`). Test: allocator attach after a "restart" passes the composed config; a live attach does not rebind. |
| R5 | major (security) | A workflow/stage `session.permissionMode: bypassPermissions` outranked an automation's declared mode and needed only `write:workflows` | **Fixed.** For a trigger-started run the automation's mode is a ceiling over the definition layers (the more restrictive wins; an explicit, admin-gated run-row mode still decides). A workflow or stage session set to bypass is command-bearing (`kind: 'bypass'`), so saving it needs `admin:settings`. Test: the layer test's bypass-over-trigger expectation flipped to the trigger's mode. |
| R6 | major (security) | Computer use under bypass was checked only at compose: a PATCH to bypass left `computer_*` bound and auto-allowed; later stages on a shared conversation inherited the first stage's computer tools and tool-group wrapper | **Fixed.** `TurnContext.policy` (`computerUse: switch | opted_in | off`, the owner's own `groups`) is stamped on every stage turn (`ComposeResult.turnPolicy`; SES keeps one per stage run, `off` when not composed in this process). Every `computer_*` handler checks the turn in flight and refuses on bypass or without opt-in; the tool-policy wrapper applies the in-flight owner's groups. Test: after binding, a bypass turn and a non-opted-in owner are both refused at call time. |
| R7 | major | Stage question / plan-review gates could not be answered from the run page, and the approve route re-injected the answer as a follow-up (delivered twice) | **Fixed.** The approve route treats `tool_permission`, `question` and `plan_review` like `stage_completion_review`: the value alone answers the parked turn, no follow-up reservation or injection; a gate's structured value wins and a free-text follow-up rides along as the freeform answer / feedback. The stage `StreamPanel` gets `onAnswerPermission`, `onAnswerQuestion`, `onApprovePlan` and `onRequestPlanChanges`, wired to `/stages/:id/approve` (a denied call is `changes_requested`, never the run-ending `rejected`). Test: route answers a question gate with `{answers}` and never calls `sendStageFollowUp`. |
| R8 | major | PD-17 was not always refused at run start: `assertPermissionGating` ignored the bound agent's runtime harness, and the mode PATCH did not re-check | **Fixed.** The start check resolves each stage's provider through the agent resolver (the agent's runtime harness/model, as the composer folds it), then routing by model; `setPermissionMode` runs the same check with the new mode before writing. Tests: an agent whose runtime is opencode refuses `default` at start; a mid-run switch to `default` on opencode is refused. |
| R9 | major | The whole-run tests the phase requires were missing | **Fixed.** Testkit scenarios `P02-perm` (claude-agent and codex stages receive the run's `default` mode on every turn and a permission gate) and `P02-mcp-secret` (a stage's `secretref:` MCP header reaches the provider resolved). The testkit records per-turn options, takes a run `permissionMode`, and wires the MCP hub + vault when given `secrets`. Widgets, skills and question scenarios stay unit-level (DEVIATIONS). |

**Known gaps (not fixed here):**
- RV-7: no chat golden fixture covers skills (the plugin delivery is unit-tested in `session/agentProjection.test.ts` and the providers' workspace-mounts test; a chat golden case with a skilled agent is still to add).
- Plugin roots are per conversation and are not reclaimed when a conversation ends (they live in the workspace's managed root and go with the workspace).
