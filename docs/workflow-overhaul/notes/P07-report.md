# P07 report: economy, flow keys, tracing alignment, UX (for the orchestrator to merge)

**Branch:** `wf/p07` (worktree `C:/gaiwf/p07`), from `wf/overhaul` @ `52e5497`, in parallel with P06.
**Commits:** 3a93d7f, f78ac16, 6cae36b, 12d815c, 92449d5, af70b6d, 1ead2c0, 0b6c23b, 39aed2d, 6ebfac0.
**Migration:** none (v61 stays reserved).
**Tests:** IMPLEMENTATION FIRST — no new tests. Existing tests updated where P07 changed behaviour (testkit smoke/T1/T5/T7: short outputs pay no summary turn; T7's A output made > 6,000 chars so the model summary path stays covered; workflow-spec stage defaults include `output.summary`; two server e2e assertions for `search`). Two measurement benches added (not tests): `packages/workflow-testkit/bench/stage-overhead.bench.ts`, `apps/server/bench/stream-rows.bench.ts`.

## 1. TRACKER rows (Phase 07)

| WP | Task | Status | Review | Commit | Notes |
|---|---|---|---|---|---|
| 7.1 | Turn economy | done | todo | 3a93d7f | `output.summary: none/auto/llm` (default auto) in the spec (FIELDS.md, JSON Schema, templates regenerated). auto: json → its `summary` field or its keys; text → first ~1,200 chars + headings; one summary turn only when a successor reads the summary AND the output > 6,000 chars. llm: `summarize` effect (`services/engine/summaries.ts` `SummaryEffects`, tool-less one-turn conversation on the workflow summary model from Settings → Workflow engine, else the stage's model); only summary readers wait (`awaitsSummary` in readiness; `summary_ready` message; the run counts as busy meanwhile); failure → deterministic fallback; recovery re-dispatches. The "How to create files" block and the fenced-block file scrape were already gone (P03); the remaining intent: a stage without fileWrite/shell tools gets the `[Workspace]` directories without the file-placement rules (chat prompts unchanged, golden snapshots pass). Checkpoints: `CheckpointService.create` skips outside the per-repo lock when `git status --porcelain` is clean and HEAD's tree is the previous snapshot (not for shadow-store in-place folders). Bench: 16.6 ms engine overhead per stage (10-stage chain, faux provider; target < 150 ms), soft gate at 1.25x `bench/baseline.json` (40 ms). Live T10 (advisory) not run. Tests deferred to final pass (the summary policy, the llm wait, the checkpoint skip). |
| 7.2 | Flow keys | done | todo | f78ac16 | `AdmissionController` flow keys: all-or-nothing admission, FIFO per key, limits changed live (`setFlowLimits`); defaults `global` (ordinary-lane sized), `provider:claude-agent` 4, `check:global` 2; `model:<id>` only when configured. Stage launches are admitted on flow keys only (`RunSupervisor.flowKeysOf`: check → `check:global`; agent → `global` + `provider:<harnessType or routed provider>` + configured `model:`), and announce `stage_run.admission_queued` / `admission_granted`. The hidden claude-agent cap (O-2) is `provider:claude-agent`: `AgentHostSupervisor.useExecutionGate(admission.flowGate(...))`; `GENERATORAI_MAX_CONCURRENT_AGENT_TURNS` deleted (banned). A stage attempt holds its provider key for all its turns (`SendPromptOptions.admitted`), judges ride on it. Agent host (RV-26): `AgentHostClient.useTurnGates` admits every turn on the gateway's key and sends it `admitted`; the host skips its permit for admitted turns (`GENERATORAI_AGENT_HOST_TURN_LIMIT` backstop, default 4). Settings: `<dataDir>/workflow-engine.json`, `GET/PUT /api/settings/workflow-engine` (read:workflows / admin:settings; flows live incl. `worktree:<mountId>` leases and `run:<id>` maxParallel), `/api/health` `flows`. Trigger debounce (webhook/cron): an identical trigger within the window returns the first execution. SDK/testkit `maxConcurrentStages` set `global`. UI in af70b6d (settings section, "ready · waiting for provider claude-agent (4/4)"). Tests deferred to final pass (property test: in-flight ≤ every key's limit, agent host on and off). |
| 7.3 | Budgets UI and cost | done | todo | 6cae36b, af70b6d, 1ead2c0 | `harness.usage.costUsd` only from providers that report dollars (claude-agent `total_cost_usd`; faux for tests); the engine reads `costUsd` only (Copilot's `cost` is a premium-request multiplier). No pricing table. `workflow_run.budget_exhausted` event + push notification (approval category). Run header: turns/tokens/wall clock vs budget, $ only when reported, "Budget exhausted" badge, "Raise budget 50%". Found and fixed: `maxTokens` was never enforced (`overBudget`); a `budget_exhausted` run had no way forward → run-level `raise_budget` (no instanceId) adds deltas to the run budget, re-arms a raised wall clock, resumes the run when under budget (`workflow_run.budget_raised`). Run read model carries `usage`. Tests deferred to final pass. |
| 7.4 | Tracing alignment | done | todo | 12d815c | `shared/telemetry/genai.ts` (gen_ai.* names, span helpers, `GenAiTurnSpans`: `chat <model>` per turn with request/response model, conversation id, gen_ai.usage.*; `execute_tool <tool>` children); claude-agent and copilot turns use it (old `claude_agent.sendPrompt*` / `copilot.sendPrompt*` span names banned); claude-agent feeds usage + tool events. `EngineTelemetry`: `workflow.run`, `workflow.loop`/`workflow.iteration`, `workflow.map`/`workflow.item` from the outbox events; `invoke_agent <stage>` per attempt whose context the turns run in (provider spans nest). Content capture only with `OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT=true`. Metrics: workflow.loop.iterations, workflow.loop.cost_usd, workflow.stage.attempts, workflow.stage.repairs, workflow.scheduler.decision_latency_ms, workflow.scheduler.cas_conflicts. Checked by hand (in-memory exporter on the testkit: run → invoke_agent a/b). Span-tree snapshot test deferred to final pass. |
| 7.5 | Write amplification measurement | done | todo | 92449d5 | `streamRowsFor` (server `composition/streamScopes.ts`); `workflow.stream.rows_per_event` recorded for every workflow event; `apps/server/bench/stream-rows.bench.ts` (T1 on the testkit through the server's fan-out rule): **280 rows / 140 events = 2.00 rows per event** (workflow_run 26/52, stage_run 16/32, harness 98/196), vs 3 writes per event in F T9. **W-42 closed: accepted, measured.** Soft check = warning above 2.2. Server takes the testkit as a devDependency (pnpm-lock +14 lines). |
| 7.6 | UX gaps | done | todo | af70b6d | Builder: `{{` autocomplete on every template field; inline issues on nodes/panel with quick-fixes (declare variable, rename key, remove edge, clear field, move to top level, add body stage, use JSON output) + debounced server /validate; version history (list, diff two versions or vs editor, restore as unsaved draft); searchable "Add stage" menu (kinds + stage templates; no slash commands). Run page: per-stage history across runs (`GET /api/workflow-runs/stage-history`), Usage tab, raw event log per stage (Inspector "Events"), budgets header, admission-wait label. Lists: `GET /api/workflow-runs` filters status/trigger/from/to/q/var/limit/definitionId (SQL) + filter bar on the definition page. Settings → Workflow engine. Web only (PD-19 existing components). Playwright checks deferred to final pass. |
| 7.7 | Docs | done | todo | 0b6c23b | `.github/docs/feature-workflow-operations.md` (flow keys, settings route, provider permit, agent host, debounce, budgets and cost, raise budget, summary policy, span tree, content capture, metrics, measurements); doc site `operations/workflow-engine.md` + settings row; OpenAPI `/api/settings/workflow-engine` (stage-history and run filters in af70b6d); AGENTS.md and feature-workflow-runs.md link it; environment reference updated. |

Phase summary row: `| 07 | Economy, observability, UX | done | todo | pass (2026-09-26, affected packages) | 3a93d7f..6ebfac0 | WP-7.1–7.7 done (IMPLEMENTATION FIRST, no new tests). Overhead 16.6 ms/stage; W-42 accepted at 2.00 rows/event; no migration. See notes/P07-report.md. |`

## 2. DEVIATIONS rows (Approved by: agent (pending review))

| Phase/WP | Plan says | Done instead | Why |
|---|---|---|---|
| P07 WP-7.1 | Move the "How to create files" block into the system message for write stages; delete the fenced-block scrape | Both were already gone (P03 W-48, SES deleted). Only the `[Workspace]` file-placement rules are now limited to stages with fileWrite or shell tools | The code already met the rest of the intent |
| P07 WP-7.1 | Incremental checkpoints, skipped when `git status --porcelain` is empty | Skip when the status is clean AND HEAD's tree equals the previous snapshot, decided outside the per-repo lock; shadow-store (in-place folder) mounts always take the full capture | A clean status alone does not mean "same as the last snapshot" (commits); a shadow store's status is not the user's |
| P07 WP-7.1 | `bench/stage-overhead.bench.ts` | `packages/workflow-testkit/bench/stage-overhead.bench.ts`, measured from `workflow_run.running` to the last `stage_run.completed` (the prepare/finalize phases are run-level); baseline 40 ms for a loaded CI box (16.6 measured) | The testkit is where the faux engine lives |
| P07 WP-7.2 | Flow keys in addition to the lanes | Stage launches use flow keys only; `global` takes the ordinary lane's machine sizing. `admit(lane)` now has no production caller (lanes kept for the W18 health snapshot) | One gate (W-66); remove or re-purpose the lanes in the final review |
| P07 WP-7.2 | `provider:<id>` per turn | Chats take it per turn; a stage attempt holds it for the whole attempt (judges included, even on another model); amendments and `llm` summaries take per-turn permits | A stage must show `ready · waiting for provider …` before it is claimed, and must not count twice |
| P07 WP-7.2 | Agent host enforces the same keys over IPC | The gateway admits every host turn on its own flow gate and sends it `admitted`; the host skips its permit then (its semaphore is a backstop for unflagged turns, `GENERATORAI_AGENT_HOST_TURN_LIMIT`) | One semaphore owner, no permit round-trip over IPC |
| P07 WP-7.2 | Debounce for webhook and cron triggers | Leading-edge coalescing of identical triggers (same automation, same trigger kind, same payload) within `triggerDebounceMs` (default 2 s) | A webhook caller always gets an execution id and no distinct payload is dropped |
| P07 WP-7.3 | Budgets UI | Also: `maxTokens` enforcement (it was never checked) and a run-level `raise_budget` | A run paused with `budget_exhausted` could not continue (resume re-paused it) |
| P07 WP-7.4 | `chat`/`execute_tool` from the providers | claude-agent: chat + tool + usage; copilot: chat spans only (its events reach each subscriber separately, observing them would double-count); codex/opencode/acp have no spans (they never had) | Alignment, not greenfield (RV-36) |
| P07 WP-7.5 | Soft CI check on the testkit T1 run | `apps/server/bench/stream-rows.bench.ts` (bench mode; warning only) — the fan-out rule is the server's, so the server takes the testkit as a devDependency | The rule must not be duplicated into the testkit |
| P07 WP-7.6 | UX gaps | Web only; mobile/TUI keep their P05 rows (the run filters are available to them through client-core). The admission label comes from live events (not after a reload). Version restore does not undo workflow settings | PD-19; scope |
| P07 no-legacy | Bump `comments.phase` | Left at 03b; P07 bans inserted at the top of `banned` | Keep the merge with P06 conflict-free; bump when merging |

## 3. TRACEABILITY (closed or advanced)

| ID | Closed by | Evidence |
|---|---|---|
| W-49 (rest) | 3a93d7f | summary policy (auto = no turn for short outputs; llm off the critical path), read-only stage hint, lock-free checkpoint skip; 16.6 ms/stage bench |
| W-66 (rest) | f78ac16 | stage launches admitted on flow keys only; the provider turn permit is the `provider:claude-agent` flow key; `GENERATORAI_MAX_CONCURRENT_AGENT_TURNS` banned |
| W-42 | 92449d5 | accepted, measured: 2.00 rows per event on T1 (≤ 2.2) |
| F O-1 | 3a93d7f | clean-checkout checkpoint skip outside the lock |
| F O-2 | f78ac16, af70b6d | the 4-turn cap is a visible, configurable flow key; the run page names it |
| F O-3 | 3a93d7f | no summary turn under auto unless needed |
| F O-4 | 12d815c | decision latency metric (observability; the gaps were already small) |
| F O-5 | — | attempt history was kept since P03 (`stage_attempts`); per-attempt `invoke_agent` spans and the stage history tab show it |
| F O-6 | 3a93d7f | no file-placement rules for read-only stages |
| D §f gaps | af70b6d | per-node history, raw event log, inline validation, `{{` autocomplete, version history, run search/filters, usage roll-up, "Add stage" menu (no palette). Still open (backlog/P08): run diff, dry-run a stage, re-run from node is P05 |

## 4. STATUS / gate (2026-09-26, worktree C:/gaiwf/p07)

- **Typecheck:** `pnpm turbo typecheck` 51/51.
- **Lint:** `pnpm lint` exit 0 (security, durability, docs, syncio, tokens, workflow-invariants, no-legacy **118 bans, 0 hits, 0 legacy comments**, migrations-lock head v60, db-baseline v60 up to date, workflow-spec generated up to date, templates up to date).
- **Tests (affected packages):** core 1680 pass / 1 fail (KokoroTtsEngine real-library test: `Unexpected end of JSON input` from the cached voice model in node_modules — environment, unrelated); workflow-testkit 47/47; web 635/635; client-core 293; server 536/537 (CSP hash — recorded baseline); agent-harness-providers 740/741 under turbo load (HarnessRegistry cache overlap test; 8/8 alone — flake); workflow-spec 364/364; db 145 pass / 4 skip, `BaselineFreshDb` suite does not load; cli-core 879/879; mobile 1097/1097; shared, sdk, auth, agent-host, checkpoints pass.
- **Environment (not code):** this worktree was checked out with `core.autocrlf=true`, so shebang `.mjs` scripts are CRLF and vite's import lexer rejects them: `test:scripts` (4 suites) and db `BaselineFreshDb` fail to load. Converting `scripts/workflow-backup.mjs` to LF makes `workflowBackup.test.mjs` pass 4/4 (restored after; no index change). Re-run these in the main worktree.
- **Fresh-DB:** no migration; `check:db-baseline` up to date (the BaselineFreshDb suite is blocked by the CRLF issue above).
- **Live E2E / T10:** not run (advisory).

## 5. Tests deferred to the final pass

Property test: in-flight count never exceeds any flow key's limit (agent host on and off); the flow gate's all-or-nothing and FIFO-per-key rules; the summary policy (auto thresholds, llm wait and fallback, recovery); the checkpoint clean skip; `raise_budget` at run level and `maxTokens`; the trigger debounce; the span-tree snapshot; Playwright: `{{` autocomplete, version restore, run filters, the flow-key settings.
