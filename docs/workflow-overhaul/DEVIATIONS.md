# Deviations from the plan

Record every intentional deviation **before** implementing it. Use this format:

| Date | Phase / WP | Plan said | Doing instead | Why | Approved by |
|---|---|---|---|---|---|
| 2026-09-24 | all / README §0.4 | One branch per phase, each merged to the base branch | One integration branch `wf/overhaul` in the git worktree `C:/gaiwf/repo`, with one commit per WP and a `wf-phase-NN-done` tag per phase. Local only, never pushed. | The agent implements every phase end to end in one session. A short path avoids git long-path and OneDrive problems (R-9). The user's main checkout and any running :3100 server are untouched. | product owner ("complete everything end to end") |
| 2026-09-24 | P08 | Phase gated by PD-21 | WP-8.3 and WP-8.4 ship. WP-8.1, 8.2 and 8.8 stay gated (PD-21 default: deferred). WP-8.5–8.7 stay backlog. | Follows the plan's own defaults. | plan default |
| 2026-09-24 | P00 / WP-0.6b | `migrateDB`: an empty DB uses the baseline; existing DBs keep the legacy bootstrap + versioned path | A DB already at or above `BASELINE_VERSION` runs only the versioned migrations above its version (the legacy bootstrap is skipped). Empty DB → baseline; older DB → legacy path, unchanged. | R-3 says the bootstrap "runs only for pre-baseline existing DBs". Without this gate, a DB created from the v55 baseline would re-create dropped legacy tables (`CREATE TABLE IF NOT EXISTS workflows …`) on its next boot. The bootstrap is all `IF NOT EXISTS` / swallowed `ADD COLUMN`, so skipping it at v54 is a no-op today (tested). | agent (pending review) |
| 2026-09-24 | P00 / WP-0.6b | `baseline.sql` is what `migrateDB` applies | The generator writes `baseline.sql` (the reviewed artifact) AND `baseline.generated.ts` (the same SQL as a string constant + `BASELINE_VERSION`), which `migrateDB` imports. A test keeps them identical. | The desktop app bundles the server into one file, so a `.sql` file read through `import.meta.url` would not exist at runtime. | agent (pending review) |
| 2026-09-24 | P00 / WP-0.6b | v52 fixture "copied from a backup, anonymised" | Revised after the P00 review (R10). The committed fixture is the **schema** of the real developer DB at v52 (`packages/db/__tests__/fixtures/schema-v52-dev.sql`, dumped from the copy with `pnpm workflow:dbcopy-upgrade --dump-schema`: DDL + migration names, no user rows) plus synthetic chats, sessions and messages. It upgrades via the legacy path with the rows unchanged and converges on the fresh schema up to an explicit 8-entry drift allowlist that P01 must empty. The real rows are checked by `pnpm workflow:dbcopy-upgrade` (hashes before/after) on a copy. | The developer DB is ~310 MB of personal chats and cannot be anonymised cheaply; its schema carries no personal data. | agent (pending review) |
| 2026-09-24 | P00 / WP-0.3 | `server.mjs` takes `HARNESS_TYPE` from the environment; faux via `HARNESS_TYPE=faux` | `--provider claude-agent` (default) sets `HARNESS_TYPE`; `--provider faux` sets `GENERATORAI_LOAD_TEST_FAUX_HARNESS=true` | The server has no `faux` harness type; the load-test escape hatch is the only way to route every conversation to FauxProvider. | agent (pending review) |
| 2026-09-24 | P00 / WP-0.2 | `killAndRestart()` = dispose, re-create, recover | It also takes `{ allocatorFirst: true }`, which rehydrates `SessionAllocator` before `recover()` | `recover()` re-drives runs before it rehydrates the allocator (W-32 boot-order race). Live, the ~2.5 s checkpoint capture hides it; in the testkit the relaunch always wins, and the stage fails on the `session_allocations` UNIQUE key. The default (the code's order) characterises the race; the flag is needed to characterise W-16. | agent (pending review) |
| 2026-09-24 | P00 / WP-0.5 | Golden test with "fake deps (a harness spy …)" | The real service graph (`createCoreServices` over an in-memory migrated DB) with a spy provider and fakes only for the agent resolver, browser service and workspace manager | The snapshots have to capture what the real builders emit; hand-faking the repositories would snapshot the fakes. | agent (pending review) |
| 2026-09-24 | P00 / WP-0.8 | Exercise the cleanup script on a DB copy | Exercised `--dry-run` on the copy of the developer DB, and the real (destructive) path on a synthetic git fixture in a test | The copy's rows point at the developer's real `~/.generatorai` clones and worktrees; a non-dry run against the copy would delete the user's real worktrees and branches. | agent (pending review) |
| 2026-09-24 | P01 / WP-1.1 | Delete the `AppConfig.workflow` knobs **and setters**, replaced by module constants | `AppConfig.workflow` is deleted and the defaults are module constants. The three timing setters (`StageExecutionService.setDefaultStageTimeoutMs` / `setHeartbeatIntervalMs`, `WorkflowRunService.setHeartbeatPolicy`) stay, documented as the test timing seam; nothing in production calls them. | The P00 testkit (the hard gate) and the core liveness tests compress time through them (20 ms tick, 2 s stale window); without a seam every characterisation scenario would take minutes. P03 replaces them with the engine's clock/timer configuration. | agent (pending review) |
| 2026-09-24 | P01 / WP-1.1 | Delete the `workflowRunStore` write-only state "and its SSE writers" | Also deleted the three stream ops that fed it (`runTimeline`, `stageTimeline`, `stageAwaitingInput`) from the client-core router, since the web run store was their only consumer | R-1: an op nothing consumes is dead code on every surface. | agent (pending review) |
| 2026-09-25 | P01 / WP-1.4 | Delete `repo_path_target`; templates switch to `{{run.codebases.<alias>.path}}` | The legacy clone path and `gitRepositories` are deleted, but `WorkflowOrchestrator`/`WorkflowRunService` still set `repo_path_target` to the primary worktree, and 3 system templates still use it. It goes with the WP-1.7 template conversion. | `{{run.codebases.<alias>.path}}` needs the Expression v2 renderer (WP-1.5) and the v2 template documents (WP-1.7); switching now would invent the v2 shape (the brief forbids it) and break the shipped templates in between. | agent (pending review) |
| 2026-09-25 | P01 / WP-1.4 | Plans record "the resolved harness" | Recorded as `IAgentHarness.conversationHarness(conversationId)` (MultiHarness: the router owner), else the configured `harnessType`, else the literal `unknown`. `AgentHostClient` (out-of-process host) does not implement the optional method yet, so it records the configured type. | Only the router knows the provider when `harnessType` is unset (it routes by model). `unknown` is honest where `copilot` was a guess; the column stays NOT NULL. | agent (pending review) |
| 2026-09-25 | P01 / WP-1.5 | "Move, don't copy": delete `packages/shared/src/config/{WorkflowDefinitionSchemas,WorkflowTemplate,WorkflowScriptSchema}.ts`, `packages/shared/src/builders/*` and core `DAGValidator.ts` in WP-1.5 | The v2 shapes, builders and graph algorithm live in `@generatorai/workflow-spec` now. The shared v1 schemas/builders and the id-keyed `DAGValidator.ts` wrapper (whose ordering, layers and cycle detection now call the spec's `analyzeGraph`) stay until WP-1.7, which deletes them with their last callers. `ConditionEvaluator.ts` and the preprocessor grammar are deleted now. | They are the shapes of the v1 store, routes, templates and script loader; replacing them needs the v2 store, `createFromSpec` and migration v55 (WP-1.6/1.7). Deleting them first would force an adapter from v2 back to v1 (R-1) or pull part C into this WP. | agent (pending review) |
| 2026-09-25 | P01 / WP-1.5 | Regex rules run on `re2-wasm` (RV-21) | A pure-TypeScript linear-time engine in the spec package (`compileSafeRegex`: Thompson NFA with Pike simulation, the RE2 subset: no backreferences or lookaround), used for save-time validation and by ResultValidator / validate_input at run time. Property-tested against `RegExp` and against catastrophic patterns. | The spec package must stay zod-only and browser-safe (the builder validates patterns in the browser); `re2-wasm` is Node-oriented, 1.1 MB, unmaintained since 2021 and not in the store. Same guarantee: linear time, no native build. | agent (pending review) |
| 2026-09-25 | P01 / WP-1.5 | Expression v2 in P01 = paths, comparisons, logic and len/count/exists/lower; P05 adds list literals and full expressions in `{{ }}` | P01 already has list literals (`x in ['a','b']` needs them), full expressions and the filters `json`, `yaml`, `bullets` inside `{{ }}`, `{{#if}}...{{else}}...{{/if}}` blocks and `\{{` escapes. Functions and filters are registries, so P05 only adds entries. A comparison with a `null` literal is a type error (it is always false; use `exists`). | Leaves room for P05 without grammar changes, as the brief requires; the rest follows G5 §4.6 and P05 §1.1. | agent (pending review) |
| 2026-09-25 | P01 / WP-1.5 | Hook phases and defaults carried over from the v1 schema | The spec splits stage phases (14) from workflow phases (15) and drops `on_client_start/stop/error/restart` and `on_permission`, which have no producer (part A leftover); `GET /hooks/phases` serves the spec catalogue and the shared `HOOK_PHASE_INFO` is deleted (the v1 stored-hook schema accepts the old phases until WP-1.7). `onExhausted` and `timeouts.queueMs/idleMs` are optional with engine defaults in `STAGE_DEFAULTS`; `expectedOutput` becomes `output.instructions`. InvocationRequest, the trigger union and RunCommand are defined now (README §3 places them here) and wired in P03/P04. | Final shapes list only phases that fire; a zod default would put a v1-unsupported value into every stage and trip the engine gate. | agent (pending review) |
| 2026-09-25 | P01 / WP-1.6 | A SQL migration with frozen schema copies under `migrations/v55/` | v55 is a JS step: `Migration` gained `run(sqlite)` (runs after `sql`, in the same transaction) and `lockFiles` (the frozen files whose contents `check-migrations-lock` hashes into the v55 lock entry). The frozen copies are the spec schemas (`migrations/v55/spec/*.ts`) and the converter (`migrations/v55/convert.ts`). Each step is guarded so a re-run is a no-op. | Converting 343 JSON definitions into validated v2 documents needs code; hashing the frozen files keeps the lock meaningful for code that is not SQL. | agent (pending review) |
| 2026-09-25 | P01 / WP-1.6 | `stage_edges(…, on, when_expr, handles_failure)`; `stage_definitions(… key, name, position_x, position_y, spec …)`; `workflow_definitions(… spec …)` | Column `edge_on` instead of `on` (reserved word in SQL); `ordinal` on stages and edges (graph order is part of the canonical document); `workflow_definitions.needs_attention` (the converter's per-definition notes, cleared on the next save); a `_migration_log` table holding v55's conversion log. | Keeps the stored graph byte-identical to what was saved and makes the migration's lossy choices visible to the user and auditable. | agent (pending review) |
| 2026-09-25 | P01 / WP-1.6 | Runs pin `definition_version_id` | `stage_runs.stage_definition_id` is replaced by `stage_key`, and `workflow_runs.definition_snapshot` is dropped: the pinned version is the snapshot. `workflow_runs.definition_version_id` is NOT NULL with an FK, so v55 purges run history (explicit child deletes, FKs off, sessions filtered by `owner_type = 'stage_run'`) instead of back-filling versions for old runs. | A stage-run row keyed by an id that a graph save may replace breaks pinning; purging runs was already in the v55 contents (STATUS migration table). | agent (pending review) |
| 2026-09-25 | P01 / WP-1.6 | v55 requires `pnpm workflow:cleanup-runs` first (STATUS backup procedure step 4) | v55 checks for a non-dry-run `cleanup.json` for the same DB under `GENERATORAI_BACKUP_ROOT` / `~/.generatorai-backups` when run worktrees or workspaces exist, and throws with instructions otherwise. `GENERATORAI_V55_SKIP_RUN_CLEANUP=1` skips the check (tests, `workflow:dbcopy-upgrade`); in-memory DBs skip it. | Purging run rows without cleaning their worktrees leaks git worktrees and branches; the tests and the DB-copy check must not touch the user's worktrees. | agent (pending review) |
| 2026-09-25 | P01 / WP-1.6 | Migrated definitions keep their data | Every migrated definition is published with version 1 (`revision` = its old `version`). `sessionMode` is dropped (logged). **Updated after the P01 review (R4):** the v1 engine gives every stage a fresh session (`workflow_runs.session_mode` is always `per-stage`, the v2 default `sessionReuse: 'fresh'`); the old graph-shape resolution (`single` for linear graphs) is removed, and shared sessions arrive with session groups (P03). **R2:** v55 validates each converted document with the frozen validator (engine v1): an undeclared bare `{{x}}` is declared as an optional string variable (logged), legacy defaults are coerced to the declared type (or dropped with a note), a schema on a text output is dropped (v1 never applied it), and every remaining error becomes a `needs_attention` note. Script-mode automations (3 on the dev copy) are disabled with a note; loop/batch automations are converted to dataSchema + defaultDataset. | The plan says existing definitions migrate as published; the v2 document has no run-level session mode, and a definition that cannot run must say why. | agent (pending review); R2/R4 per the P01 review |
| 2026-09-25 | P01 / WP-1.7 | `packages/shared` stops holding workflow schemas | `@generatorai/shared` now depends on `@generatorai/workflow-spec` (not the reverse): shared's error types, hook types and client interfaces use the spec types. The spec↔shared constant drift test moved to `packages/shared/__tests__/workflowSpecConstants.test.ts`. `writeFileAtomicRestricted` / `renameWithRetry` moved to `@generatorai/shared/node` so core and secrets share one copy. | The spec package must stay a leaf (browser-safe, zod only); shared re-using its types is the one-source-of-truth direction. | agent (pending review) |
| 2026-09-25 | P01 / WP-1.7 | Templates use `{{run.codebases.<alias>.path}}` | `run.codebases` is derived from the engine's `repo_path_<alias>` / `repo_branch_<alias>` run-state keys (hidden from `variables`); user variables with those prefixes are rejected. P04 replaces the run-state keys with mounts. | The v1 engine records worktrees only there; the typed scope is what definitions see. | agent (pending review) |
| 2026-09-25 | P01 / WP-1.7 | Hook configs follow Expression v2 templating | **Superseded by the P01 review (R3): no deviation remains.** `HookExecutor` renders script-hook `env` and `cwd` and HTTP-hook `url`, `headers` and `bodyTemplate` with the spec `renderTemplate` over the run's template scope (`variables`, `run`, `stages`), the grammar the validator checks at save; the old `{{\w+}}` interpolation is deleted. `command`/`args` stay literals. | — | agent (pending review); fixed per the P01 review |
| 2026-09-25 | P01 / WP-1.7 | `GET /workflow-definitions/schema` can serve `workflow.schema.json` | Not served. Clients import `@generatorai/workflow-spec/workflow.schema.json` (or the zod schemas) directly. | Serving it at runtime needs `zod-to-json-schema` (a dev dependency) in the server bundle, or reading the generated file next to a bundled server that does not ship it. | agent (pending review) |
| 2026-09-25 | P01 / WP-1.7 | System templates are converted to v2 | Template files keep the `*-workflow.json` suffix and become `{ id, category, graph }`; `TemplateRegistry` validates each at boot and throws (`TemplateLoadError`) outside production. | One wrapper per template carries the catalogue metadata the graph has no field for. | agent (pending review) |
| 2026-09-25 | P01 / WP-1.7 | SDK and scripts go through `createFromSpec` | `ai.workflows.create()` publishes by default (`{ publish: false }` for a draft); `ScriptFacade.materialize` publishes. Server routes create drafts; the import route publishes only for user principals. | The SDK is an in-process embedder whose callers run what they create; drafts would make every existing SDK flow need a publish call. | agent (pending review) |
| 2026-09-25 | P01 / WP-1.7 | `Stage.session.provider` secret references are resolved at run time | **Updated after the P01 review (R6):** until P02 (SessionComposer) resolves them, the v1 engine capability gate rejects `session.provider` (workflow and stage) and any `secretref:` value in MCP server `env`/`headers` with `engine-unsupported`, so no secret reference reaches the harness verbatim. The validator still enforces `secretref:` for provider credentials on engine v2. | P02 owns session composition; gating keeps v1 from passing unresolved references through. | agent (pending review); gated per the P01 review |
| 2026-09-25 | P01 / WP-1.6–1.8 | One commit per WP | WP-1.6 and WP-1.7 land as one commit (the new tables, the store and every non-web caller cannot build apart); the web callers land with WP-1.8 (the builder is the web caller); docs are WP-1.9. | The DB shape change and the store/API rewrite are one compile unit. | agent (pending review) |
| 2026-09-25 | P01 / tests | P00 KNOWN-BUG markers flip in the phase that fixes them | T4's W-47 markers flipped in P01 (v2 `json_schema` uses Ajv; `custom_script` runs `command` + `args`; `llm_validation` is not in the grammar, so its case is deleted), and T4's script check uses `node --version` (the sandbox rejects `node -e`). T8 needed a parallel root stage while linear runs shared a session; that workaround was reverted after the P01 review (R4: every stage gets a fresh session), and T7 now asserts one conversation per stage with its own model (W-09 closed on v1). T6 is rewritten on v2 documents and passes (W-20/W-25/W-30/W-64), and gains draft → test run → publish → run, the pinned-version check (W-13), revision conflict, command scope and archive-on-delete. | The fixes landed with the v2 rule shapes in WP-1.7; the tests pin current behaviour. | agent (pending review) |
| 2026-09-25 | P01 / WP-1.7–1.8 | Clients choose the start route from `orchestratorConfig` | `orchestratorConfig` is gone, so web and mobile share `needsOrchestratedStart(workflow)` (client-core): the orchestrator route when the workflow has a project, mounts codebases, or has pre/post-processing; otherwise `POST /workflow-runs`. The automation form lists published, unarchived workflows only. | Before, web used `!!projectId` on one page and `!!orchestratorConfig` on another; one rule on both clients. Automations start normal runs, which need a published version. | agent (pending review) |
| 2026-09-25 | P02 / WP-2.10 | Migrations route: DBs at or above `BASELINE_VERSION` skip the legacy bootstrap (P00 WP-0.6b) | The boundary is a fixed `VERSIONED_ONLY_FROM = 55`, not the moving baseline: every database at v55 or later runs only the versioned migrations above its version. | With the baseline at 56 a v55 database took the legacy route, whose bootstrap fails on a post-v55 schema (`no such column "order"`); the developer's DB would have failed its next upgrade. v55 is the first migration whose drops the bootstrap would undo. | agent (pending review) |
| 2026-09-25 | P02 design | `ProviderCapabilities` levels live in `packages/agent-harness-providers` | The four level fields (`approvalGating`, `hostTools`, `structuredOutput`, `skills`) are on the existing `ProviderCapabilities` port in core (`IProviderInstance.ts`); the static table `PROVIDER_CAPABILITY_LEVELS` (with the level types and `sessionCapabilityWarnings`) is in `@generatorai/shared` (`constants/providerCapabilityLevels.ts`, moved there in WP-2.11 so the web SessionSpecEditor reads the composer's table; core re-exports it from `session/capabilityLevels.ts`); each provider declares its own levels and `agent-harness-providers/__tests__/capabilityLevels.test.ts` checks them against the table. claude-agent declares `approvalGating: 'per_call'` (canUseTool reaches the session gate); W35's PreToolUse-policy honesty moved to `ClaudeAgentProvider.preToolUseGated()`. | The interface already lives in core and every provider implements it; the composer must plan before a conversation (and its adapter) exists. | agent (pending review) |
| 2026-09-25 | P02 / WP-2.2 step 8 | Host tools, gates, the hook bridge and the per-call context `{toolCallId, turnId}` cross the agent-host IPC | Every function in the spawn params (tool handlers, the three gates, hook-bridge members) crosses as a callback marker and is invoked with all of its JSON-serialisable arguments. There is no separate `{toolCallId, turnId}` envelope: tool handlers take no call context today, and the turn is resolved gateway-side from the TurnContextRegistry. `send_turn` now carries the turn options (they were dropped). | The gateway already knows the turn in flight; inventing a context argument no handler reads would be dead surface. | agent (pending review) |
| 2026-09-25 | P02 / WP-2.2–2.7 | Each WP switches both CMS and SES | Each module lands with the chat path (CMS create and resume) and its unit tests; the stage executor switches to the composer as a whole in WP-2.8 (then `resolveStageAgent`, the SES permission handler, `resolveStageTurnOptions` and the session-config block are deleted, and the W-51/W-52 golden stage snapshots flip). | The SES session block is one interleaved builder; rewriting it once, onto the finished composer, avoids an interim second copy of each module inside SES. | agent (pending review) |
| 2026-09-25 | P02 / WP-2.4 | The chat ↔ SessionSpec mapping lives in the chat repository (PD-20) | It is `core/services/session/chatSpec.ts` (`chatSessionSpec(chat | CreateChatParams)`), read by the composer; the web has the inverse (`chatParamsFromSession`) for the create dialog. The repository still stores and returns the chat's own columns. | The mapping is composer input, not storage: both the create request and the stored row need it, and the repository port has no workflow-spec dependency. | agent (pending review) |
| 2026-09-25 | P02 / WP-2.8 | The stage's frozen agent snapshot is stored on the run | It is stored on the stage run's durable artifact channel (`agent-snapshot`, scope `stage_run`), written once when the stage first composes and read by its retries and restarts; without the durable engine it is not frozen. | P03 moves it onto the attempt; the artifact channel outlives the journal, needs no migration and is already per stage run. | agent (pending review) |
| 2026-09-25 | P02 / WP-2.8 | The conversation binding key includes a tool-surface hash (RV-20) | `formatConversationBindingKey` is harness::model::agentRef::agentVersion::computerUse::permissionMode, as the chat key was; no tool-surface hash. A single-mode shared stage session keeps the config it was created with (the stage still composes for its turn options and gates; no interim rebind). | P03 owns session groups and rebinding; hashing the tool surface now would force a rebind on every widget/extension change for chats with no consumer of the difference. | agent (pending review) |
| 2026-09-25 | P02 / tests | Question and plan-review round trips survive a restart via testkit `killAndRestart` | Tested at gate level: a second `HitlService` over the same DB answers the parked question/plan review and the verdict reaches the relaunched stage's gate (session/gates.test.ts). | The v1 engine skips an interrupted mutating turn on restart (W-16, P03), so a testkit restart cannot reach the gate that was parked; the gate-level test covers the durable half P02 owns. | agent (pending review) |
| 2026-09-25 | P02 / WP-2.7 | W-59 (retry keeps the run's settings) lands in P03 | Retries (`retryRun`, stage-retry runs) now copy the ancestor's `permissionMode` in P02, with the permission-source work. | Dropping the mode on retry would have made the new permission source fall back to the posture on every retry — a regression P02 itself would introduce. | agent (pending review) |
| 2026-09-25 | P02 / tests | T8's W-32 characterisation pins the boot-order race failing the relaunched stage | Since WP-2.8 the race no longer reproduces in the testkit: a stage composes (run workspace, permission source, hooks) before `allocateSession`, so `recover()`'s rehydrate wins. T8 now pins that the run completes, marked `KNOWN-BUG W-32 (masked by timing, not fixed)`. | The ordering bug is unchanged in `recover()` and closes in P03 WP-3.6; the test records what the code does now. | agent (pending review) |
| 2026-09-25 | P02 / WP-2.11 | SessionSpecEditor offers every control on every surface | Chats get runtime, mode, agent, skills and MCP; the browser stays on the chat's existing visibility picker and orchestrate checkbox, and computer use/widgets are not offered to a chat (a chat follows the deployment switch; the chat row has no fields for them). Workflow and stage get every section. | The chat create fields (PD-20) have no computer-use or widgets column; offering a toggle that is not stored would be dead UI. | agent (pending review) |
| 2026-09-25 | P02 / review R9 | Testkit + advisory E2E scenarios `P02-perm`, `P02-mcp-secret`, `P02-widgets-stage`, `P02-skill-claude`, `P02-question-stage` | `P02-perm` and `P02-mcp-secret` are testkit scenarios (`workflow-testkit/__tests__/p02`). Widgets, skills and questions are unit-level: stage widget routing (`session/stageWidget.test.ts`), skill delivery per provider incl. the Claude plugin root (`session/agentProjection.test.ts`, providers workspace-mounts), question/plan-review round trips and restart survival (`session/gates.test.ts`). No advisory live E2E was run. | The fake harness does not render widgets, load skills or ask questions, so a whole-run scenario would only re-test the unit path through more layers; the live checks need real providers. | agent (pending review) |
| 2026-09-25 | P03 / WP-3.2 | v57 recreates the run tables per G5 §6.2; Milestone 3A stays additive | The v1 engine keeps running on the v2 tables until the part-3 cutover through a mapping in its own repositories (deleted with the v1 engine): `DrizzleStageRunRepository` stores v1 `queued` as `ready`, `retryCount` as `current_attempt`, `currentStep`/`totalSteps` in `usage.v1Steps`, `kind='agent'` and `instance_path = stage_key`; `DrizzleWorkflowRunRepository` keeps the optional v1 run permission mode in `run_overrides.permissionMode` (the NOT NULL `permission_mode` column holds it or `default`, never read by v1), reports `sessionMode: 'per-stage'`, writes `root_run_id = id`; `DrizzleSessionAllocationRepository` keeps the allocator's state in `run_sessions` (`@v1-allocation` and `@v1-stage:<id>` rows). | The final schema lands once, with no second rebuild after the cutover, while every v1 suite stays green. | agent (pending review) |
| 2026-09-25 | P03 / WP-3.2 | `stage_runs.stage_definition_id NOT NULL` (G5 §6.2) | Not added. Instances carry `stage_key` (v55): definitions and pinned versions are keyed by stage key since P01, and a version's graph has no stage ids. | One identity for a stage; G5 predates the key model. | agent (pending review) |
| 2026-09-25 | P03 / WP-3.2 | `stage_runs` has no `session_id` (G5 §6.2: attempts and `run_sessions` hold sessions) | Kept as the conversation of the instance's current attempt (FK to `sessions`). | The run page, the stage conversation API (P03b) and the v1 engine all need "the stage's conversation" without a join through attempts; the attempt row stays the source of truth per attempt. | agent (pending review) |
| 2026-09-25 | P03 / WP-3.2 | Timestamps as in the v1 tables | Every timestamp of the recreated and new run tables is epoch milliseconds (drizzle `timestamp_ms`). | One-second heartbeats reaped healthy stages (STATUS P00 timing notes); leases and timers need ms. The tables are empty after the purge, so nothing is converted. | agent (pending review) |
| 2026-09-25 | P03 / WP-3.2 | G5 `UNIQUE (…)` table constraints | Unique indexes with names (`idx_stage_runs_instance`, `idx_stage_attempts_no`, `idx_run_sessions_key`, partial `idx_workflow_runs_idempotency`). | Same constraint; named indexes are declared in `schema.ts` and covered by the fresh-DB reverse diff. | agent (pending review) |
| 2026-09-25 | P03 / WP-3.3 | Tests first: table tests, precedence, `fast-check` model tests over the ten G5 §7.2 invariants, replay fixtures (G5 §7.3) | Table and scenario tests only (78: joins, scope outcome, B probes S1/S3/S4/S5, T2, precedence, budgets, concurrency, commands, timers, determinism). The test driver checks §7.2 invariants 1 (every transition legal) and 3 (no launch outside a running/waiting run) on every step. No model-based fast-check suite and no journal replay fixtures yet. | The product owner's speed rule (2026-09-25) limits tests to table tests for the transition tables and `decide()` semantics. Replay fixtures need real `scheduler_journal` rows, which exist only once the actor runs (part 2); the model test fits best with the in-memory actor harness of WP-3.6. | agent (pending review) |
| 2026-09-25 | P03 / WP-3.3 | G5 §5.3 decision set | Four decisions added: `settle_attempt` (§5.5 step 1 as a decision), `run_patch` (the outcome of a cancelling run), `deliver_input` (a verdict to a live frame parked in `awaiting_input`), `prepare` (the P04 prepare phases after `created → starting`), plus `reject` for a refused command. Messages `prepared`/`prepare_failed`/`finalized` close the starting and finalizing phases. The `ready → awaiting_input → ready` path carries the verdict in `interrupt_data.verdict` and the next attempt's `overrides`. The next attempt's mode rides in `status_reason` (`retry:resume`, `retry:restart`, `resume`) while an instance waits for a slot. | The state machines need these steps and G5 §5.3 does not name them; keeping them as decisions keeps `decide()` the only place that chooses them. | agent (pending review) |
| 2026-09-25 | P03 / WP-3.6 | Commands API route (`POST /api/workflow-runs/:id/commands`), old per-action routes deleted, clients switched in this milestone | `RunSupervisor.command(runId, RunCommand)` is the whole commands surface (validated by `decide()`, 409-style refusals as `{code}`); the HTTP route, the deletion of the per-action routes and the client switch (web, mobile, CLI, TUI, SDK, client-core) move to the part-3 cutover (WP-3.7). | The brief for part 2 keeps everything additive and wired only through tests; a route that drives the v2 engine cannot exist while the server still runs v1. | agent (pending review) |
| 2026-09-25 | P03 / WP-3.5 | Delete the stage Semaphore (one gate, W-66) | The v2 path has no Semaphore: `EffectsDispatcher.launch` waits on `AdmissionController.admit('ordinary')` only, while the instance stays `ready`. The v1 `Semaphore` is deleted with the v1 engine at the cutover. | v1 still runs production until WP-3.7. | agent (pending review) |
| 2026-09-25 | P03 / WP-3.5 | Approval gate: `running → awaiting_input(completion_review)` | The executor validates first (`running → validating`, repairs), then goes `validating → running` (the `exec:repair` row) and `running → awaiting_input`, so a reviewer only ever sees an output that satisfies the contract. A revision (`changes_requested`) is validated again before the next round. Approval ends `awaiting_input → running → validating`, and success is reported from `validating` as for any stage. | The state table has no `validating → awaiting_input`; validating before review avoids approving an output that then fails its contract. | agent (pending review) |
| 2026-09-25 | P03 / WP-3.5 | A revision "merges into the output" (F-2, W-46) | The latest answer of the prompt, repair and revision turns IS the output text (rules, extraction and successors see it); earlier answers stay in the transcript, not concatenated into the output. | Concatenation is what made rules judge rejected answers (B-11); the reviewer asked for a revised answer. | agent (pending review) |
| 2026-09-25 | P03 / WP-3.5 | Text stages: a summary turn only when a successor needs `context.mode: summary` | Implemented as stated. A successor in `summary` mode reading a stage with no summary gets its output text (truncated at 3000 characters). `pre_run` hook context messages are delivered inside the fenced context block of the first prompt, not as separate turns. The v1 code-block scraping for `artifact_manifest` is not carried over (the manifest is null on v2 until P03b/P07 records file changes from tool calls). | No context-ack turn (F O-3); scraping fenced blocks was the fragile heuristic P02 already stopped relying on. | agent (pending review) |
| 2026-09-25 | P03 / WP-3.5 | Codex resume (`hostTools: start_only`): re-send `dynamicTools` if supported, verify first | Not verified against a live app-server: a resumed Codex thread uses native output, then the final JSON block, and the executor emits an `output_extraction` session warning. | Needs a live Codex app-server (advisory live E2E). | agent (pending review) |
| 2026-09-25 | P03 / WP-3.6 | Recovery: tool-permission interrupts inside a lost turn become `paused(interrupted)` (G5 §3.10 step 4) | An `awaiting_input` instance whose frame died is settled `aborted` and keeps its interrupt data; an approval then starts a resume attempt, which re-sends the interrupted turn after a notice (the turn never settled). A completion review resumes without re-sending anything (its turns settled). | `decide()` has no `awaiting_input → paused` pair; adding one is a state-table change for part 3 (handoff gap 3). | agent (pending review) |
| 2026-09-25 | P03 / WP-3.6 | Recovery rehydrates `run_sessions` first (B-20) | Nothing is rehydrated into memory: the executor reads `run_sessions` when it binds a session, which is after recovery settled every lost attempt, so the boot-order race cannot exist. | The allocator's in-memory map is what raced; v2 has none. | agent (pending review) |
| 2026-09-25 | P03 / WP-3.6 | `prepare` runs the orchestrator phases for every entry point (C-1) | `DefaultRunLifecycle.prepare` creates the run workspace and runs `on_run_start`; clone, preprocessing, project worktrees and the PD-17 start check join it in P04 (one lifecycle). `finalize` runs compensation, `onFailure`/`onExit` actions, the run hooks, releases every session and completes the workspace; commit/PR post-processing stays on the RV-5 terminal events until P04. | P04 owns the lifecycle; part 2 is additive. | agent (pending review) |
| 2026-09-25 | P03 / tests | G5 §7.3 replay fixtures from the `scheduler_journal` | `scheduler_journal` rows keep `{message, decisions, stateHash}` as G5 §6.2 says; the fixtures (`core/__tests__/fixtures/scheduler/*.jsonl`) are recorded by the testkit through the actor's `onDecide` tap and also carry the state `decide()` read, because executor-owned CAS writes change the state between two journal rows. Two fixtures (fan-out/join, retry then failure routing). | A replay needs the input state; the journal row stays small. | agent (pending review) |
| 2026-09-25 | P03 / tests | Testkit v2 runs are created through the v1 `WorkflowRunService.createRun` | The v2 adapter creates the run row and pending instances with `createRun` (it stays the create path until P04, PHASE-03 WP-3.7) and starts it with `RunSupervisor.startRun`; `decide()` adopts the instances by stage key. The engine-neutral `pause` command maps to `pause {mode: interrupt}`; `retry-run` (fork, WP-3.8) and the test-only `interrupt` answer 501 on v2. | Part 3 replaces create and adds fork. | agent (pending review) |
| 2026-09-26 | P03 / WP-3.7 | Remove the v57 v1 mapping in the run repository, including `run_overrides.permissionMode` | The v1 mappings are gone (`queued↔ready`, `retryCount`, `usage.v1Steps`, `sessionMode`). `run_overrides.permissionMode` stays as the **operator's explicit run-level mode** (the run-row layer of the permission source, optional); the NOT NULL `permission_mode` column holds the run's **effective** mode as resolved at create or last change (`WorkflowRun.effectivePermissionMode`). | The permission layers (run row → stage → workflow → trigger → posture) need "no explicit run mode" to be representable; a NOT NULL column cannot say that, and P04's invocation writes the resolved mode the same way. | agent (pending review) |
| 2026-09-26 | P03 / WP-3.7 | Purge the `@v1-allocation` / `@v1-stage:` rows of `run_sessions` in the cutover commit | No purge migration. The rows exist only in a database the interim v1 engine ran on after v57 (test databases); the developer DB copy never ran v1 on v57, and the user's DB is below v57. A stale row is inert (the engine reads `run_sessions` by run and session key; v1 keys never match). | A data migration needs a version number, and v58 is reserved for P04 (RV-17). | agent (pending review) |
| 2026-09-26 | P03 / WP-3.7 | `WorkflowRunService.create` stays (P04 replaces it); "better: insert no pending instances" | `createRun` inserts the run only; `decide()` creates every top-level instance with a deterministic id when the run starts. `failSetup(runId, phase, error)` fails a run whose pre-start orchestration (clone, preprocessing) failed through the legal CAS path `created → starting → failed` with `status_reason = setup:<phase>`, and emits `workflow_run.failed`. | The orchestrator's clone/preprocess still runs before `start` until P04 moves it into `prepare`; a run is never failed by a direct status write. | agent (pending review) |
| 2026-09-26 | P03 / WP-3.7 | `HitlService` works on instances: resolutions post `command approve`, cancel calls `cancelWaiter` (B-4) | `HitlService` is a thin operator facade: `listPending` (the `awaiting_input` instances), `resolve` (the `approve` command), `cancel` (the `cancel` command, whose abort releases the parked frame's waiter). The durable-awakeable wait, `interrupt`, `resume`, `cancelWaiter` and the restart-verdict stash are deleted: stage gates park the executor frame (`StageGatePort.park`), and the verdict of a lost frame travels on the resume attempt. The SDK's `ai.hitl` becomes `pending` / `resolve` / `cancel`. | Chats use `AgentInteractionService`; no caller of the old waiter API remains. | agent (pending review) |
| 2026-09-26 | P03 / WP-3.7 | Recovery gap 3: a tool permission lost to a restart becomes `paused(interrupted)` | Added the `awaiting_input → paused` row (`sched:frame_lost`, actor, work nodes) and the `frame_lost` message. Recovery posts it for every `awaiting_input` instance without a frame; an in-turn gate (tool permission, **question, plan review**) pauses the instance (`status_reason = interrupted`, the request cleared) and a resume re-sends the turn; a completion review stays parked as before. | G5 §3.10 names tool permissions; a question or a plan review inside a lost turn is the same case (the turn never settled). | agent (pending review) |
| 2026-09-26 | P03 / WP-3.7 | `StartupRecoveryService` is deleted; the supervisor owns recovery | The non-run half of the old service (EventBus counter restore, sessions left `closing`, sandbox orphans) moves to `runBootHousekeeping`; eager rehydration of non-chat sessions is dropped (the executor resumes a stage conversation with its tools when it binds). | Run recovery is the supervisor's; the rest still has to happen at boot. | agent (pending review) |
| 2026-09-26 | P03 / WP-3.7 | Review threads delivered to a stage (`stage_followup` target) | Delivered as `approve {changes_requested, feedback}` when the stage is parked on its completion review; otherwise 409 `STAGE_NOT_AWAITING_REVIEW`. A follow-up on a finished stage arrives with the stage conversation API (P03b). | `sendStageFollowUp` was a v1 SES path with no v2 equivalent until P03b. | agent (pending review) |
| 2026-09-26 | P03 / WP-3.7 | A stage override skips or re-parameterises a stage (v1 `findStageOverride`) | Kept working at the cutover instead of waiting for P04: overrides are stored in `stage_overrides`; `decide()` skips an overridden stage when it becomes ready (`skip_reason = operator`), and the executor merges its `variables` into that stage's attempts. | Deleting v1 would otherwise have dropped a shipped run option (web run dialog, CLI profiles, orchestrator). | agent (pending review) |
| 2026-09-26 | P03 / WP-3.7 | `DELETE /workflow-runs/:id` cancels a live run, then deletes it | A live run is refused (409, "cancel it first"); `created` and terminal runs are deleted. | Cancellation is asynchronous in the engine (it finalizes); deleting the rows under a live actor would race it. | agent (pending review) |
| 2026-09-26 | P03 / WP-3.7, 3.8 | Commit per WP | WP-3.8 (fork core, route, clients' "Retry failed") lands in the WP-3.7 cutover commit. | R-2: the per-action `/retry` route is deleted and every client switched to fork in the same commit, so fork has to exist in it. | agent (pending review) |
| 2026-09-26 | P03 / tests | Flip the P00 characterisation tests; T2, T4, T6, T7 still needed v2 ports | The v1 adapter and `__tests__/current-engine/` are deleted; the suite is `__tests__/engine/` on the (only) engine: T1/T3/T5/T8 are the part-2 v2 scenarios, T2/T4/T6/T7 are ported (T4 now pins W-17/F-5/W-02 fixed, T7 W-48/W-49 fixed), plus `fork.test.ts` (WP-3.8, W-12/W-59/B-6) and a T8 case for a tool permission lost to a crash. The testkit's `TestEngineTiming` is the supervisor's lock/reaper timing. | Same intent: every KNOWN-BUG of the phase's ids asserts the correct behaviour, on the engine that runs. | agent (pending review) |
